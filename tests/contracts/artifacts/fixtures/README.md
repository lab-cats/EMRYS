# Artifact-contract fixtures

These literal reviewed inputs exercise current artifact entries and result
manifests under [artifact_schema_v2](artifact_schema_v2/README.md), and the
current [report receipt](report_receipt_v5.json). Directory names do not select
schema versions. Their scientific claims and failure protection belong to the
[artifact-contract tests](../README.md); they are not evidence that real artifacts exist.
