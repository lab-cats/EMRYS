# GitHub Actions workflows

[`ci.yml`](ci.yml) runs the repository's revision-bound engineering checks. Pull requests run the ordinary checks; scheduled or manually selected lanes add longer synthetic and scheduler evidence. A green workflow does not establish institutional-site, production, scientific-review, or biological proof.
