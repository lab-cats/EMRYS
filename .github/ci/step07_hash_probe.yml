schema_version: emrys.resource-benchmark.v1
cases:
  - name: step07_direct_actual
    values: [1, 8, 25]
    repetitions: 3
    setup_argv: null
    producer_argv:
      - .venv/bin/python
      - .github/ci/step07_hash_probe.py
      - produce
      - --mode
      - direct-actual
      - --partitions
      - "{value}"
      - --trial-dir
      - "{trial_dir}"
    validator_argv:
      - .venv/bin/python
      - .github/ci/step07_hash_probe.py
      - validate
      - --mode
      - direct-actual
      - --partitions
      - "{value}"
      - --trial-dir
      - "{trial_dir}"
  - name: step07_direct_no_read
    values: [1, 8, 25]
    repetitions: 3
    setup_argv: null
    producer_argv:
      - .venv/bin/python
      - .github/ci/step07_hash_probe.py
      - produce
      - --mode
      - direct-no-read
      - --partitions
      - "{value}"
      - --trial-dir
      - "{trial_dir}"
    validator_argv:
      - .venv/bin/python
      - .github/ci/step07_hash_probe.py
      - validate
      - --mode
      - direct-no-read
      - --partitions
      - "{value}"
      - --trial-dir
      - "{trial_dir}"
  - name: step07_reuse_actual
    values: [1, 8, 25]
    repetitions: 3
    setup_argv: null
    producer_argv:
      - .venv/bin/python
      - .github/ci/step07_hash_probe.py
      - produce
      - --mode
      - reuse-actual
      - --partitions
      - "{value}"
      - --trial-dir
      - "{trial_dir}"
    validator_argv:
      - .venv/bin/python
      - .github/ci/step07_hash_probe.py
      - validate
      - --mode
      - reuse-actual
      - --partitions
      - "{value}"
      - --trial-dir
      - "{trial_dir}"
  - name: step07_reuse_no_read
    values: [1, 8, 25]
    repetitions: 3
    setup_argv: null
    producer_argv:
      - .venv/bin/python
      - .github/ci/step07_hash_probe.py
      - produce
      - --mode
      - reuse-no-read
      - --partitions
      - "{value}"
      - --trial-dir
      - "{trial_dir}"
    validator_argv:
      - .venv/bin/python
      - .github/ci/step07_hash_probe.py
      - validate
      - --mode
      - reuse-no-read
      - --partitions
      - "{value}"
      - --trial-dir
      - "{trial_dir}"
