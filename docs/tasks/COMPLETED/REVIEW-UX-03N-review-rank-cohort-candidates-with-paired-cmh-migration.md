# REVIEW-UX-03N — Review paired-CMH analysis migration usability

## Objective

Review `MIG-03N` for operator, maintainer, automation, recovery, statistical-
evidence, R-environment, and scientific-language continuity across every
explicit Step `09` path change.

## Why this exists

The migration changes Bash, Rscript, validator, submitted-job, guarded-R,
independent-oracle, Make/test/helper, and implementation-provenance paths for a
six-file transaction. Correct relocation can still leave stale commands,
hidden R/input/output/lock/summary selection, incorrect dry-run claims, unsafe
retry guidance, scientific overclaim, or an undiscoverable final owner.

## Fixed decisions

- Review only; do not redesign arguments, tables, messages, R behavior,
  pairing, CMH/BH/status policy, transaction, scheduler policy, artifact
  meaning, or evidence state.
- Preserve explicit repository-relative and absolute-path invocation without
  installation, ambient import discovery, global `sys.path`, or a legacy alias.
- Keep fake-R/guarded-real-R/independent-oracle migration evidence distinct
  from scheduler, cluster, production, completed scientific review, validated
  RNA-editing sites, or biological-readiness proof.

## Blocked by

- [REVIEW-REL-03N](../COMPLETED/REVIEW-REL-03N-review-rank-cohort-candidates-with-paired-cmh-migration.md) — Required: usability review needs the corrected architecture and reliability obligations.

## Completion unblocks

- [MIG-03N](MIG-03N-migrate-rank-cohort-candidates-with-paired-cmh-owner.md) — Fully: completed after all three reviews closed.

## Prerequisites

- Inspect the committed reliability-reviewed cards against public Bash/R/
  Python CLI, arbitrary-CWD, guarded-R/oracle, scheduler submission, Make,
  runbook/troubleshooting, artifact, evidence-status, and rollback journeys.

## Required context

- `MIG-03N`; Step `09` runbook/troubleshooting commands; shell/R/validator
  help; public CLI and scheduler characterization; guarded-R/local-R and
  independent-oracle routes; Make/literal expansions; coverage/artifact/helper
  paths; owner contract; current/future topology; manifests; Step `08`/R/
  output/lock/summary diagnostics; scientific-language constraints; and the
  six-file transaction evidence boundary.

## Questions owned by this card

- None.

## In scope

- Root/arbitrary-CWD shell dry-run/execute and validator dry-run/execute/repeat
  journeys; direct Rscript, guarded-R, and independent-oracle test journeys;
  Rscript/R-program, manifest, Step `08`, output, scratch, lock, and summary
  selection; CMH-ranked-candidate and non-biological wording; staged
  publication, restoration failure, mutation, relative-path disagreement,
  residue, and safe preservation; scheduler submit CWD, modules, logs,
  delegation, and stale outputs; Make/test commands; implementation/evidence
  provenance; owner findability; links; rollback; and next-safe-action
  instructions.

## Out of scope

- New aliases/wrappers, package installation, PATH/`PYTHONPATH` redesign,
  R/schema/method/statistical-policy changes, transaction or recovery redesign,
  scheduler hardening, cluster submission, dependency action, scientific/
  biological interpretation, or future units.

## Deliverables

- Journey-based findings with exact card/documentation corrections and dated
  audit dispositions.

## Acceptance evidence

- Every supported healthy/failure transition has one final command, owned
  diagnostic, artifact expectation, preservation route, and evidence ceiling.
- The owner README and runbook make Bash/R/validator/scheduler, distinct dry-
  run effects, pairing/statistical meaning, R/input/output/lock/summary
  selection, restoration residue, guarded-R/oracle/focused tests, evidence
  status, provenance, and rollback discoverable without an alias or overclaim.

## Canonical documentation updates

- This card, `MIG-03N`, current roadmap/handoff where status changes, and the
  dated refactor log.

## Escalation conditions

- Stop if continuity requires a legacy path, changed public interface,
  dependency installation, public import identity, statistical/scientific/
  biological claim, or an unreviewed alias/package contract.

## Completion record

Completed against clean, published, local/upstream/live-remote-equal selection
checkpoint `cd8930c1bc6e43d43e0966df4e773ce03c85168b`.

- **High — four supported journeys need exact final paths and prerequisites:**
  at documentation close replace every live shell, R, validator, job, direct-
  test, helper/oracle/corpus, artifact, and coverage path. Root shell use
  invokes the mode-`0755` final producer at
  `src/norad/analyses/rank_cohort_candidates_with_paired_CMH/step_09_cmh_editing_site_calling.sh`;
  its sibling R program is selected by default, and direct Rscript use is a
  maintainer/fixture diagnostic rather than a transaction bypass. Invoke the
  mode-`0644` validator with an explicit interpreter from the same owner.
  Submit the mode-`0755` owner-local job with `sbatch` from the checkout only
  after `mkdir -p logs`; explicit Bash is a local wrapper diagnostic, not
  scheduler execution. From another CWD, make the producer/validator/job,
  interpreter, Rscript, R program, both manifests, Step `08` root or exact
  tables, output root, all six native outputs, and report path absolute. No
  installed command, public package/import, legacy alias, wrapper, symlink,
  ambient `PYTHONPATH`, or global `sys.path` route is supported.
- **High — shell, validator, R/oracle, and scheduler effects must remain
  distinct:** shell dry-run validates identifiers, pairing, manifests, Step
  `08` receipt/sites, thresholds, Rscript, and R-program resolution; prints the
  exact R command; invokes no R; and creates no output directory, lock,
  scratch, backup, or final. Validator dry-run snapshots ten explicit inputs,
  prints seven rows, invokes no R, and writes no report; execute requires the
  exact report parent and may return `0` with failed rows. The owner-local
  guarded-real-R runner and independent count-derived oracle are separate
  semantic fixture evidence, not publication, scheduler, cluster, or
  production proof. Scheduler `EXECUTE=0` changes to the submit/fallback CWD,
  creates body-level `logs/`, tolerates `module list`, performs no Rscript
  version probe or package activation, and delegates shell dry-run. SLURM opens
  declared log paths before the body, so `logs/` must already exist. Execute
  can accept six stale nonempty outputs after a zero-output child; scheduler
  exit `0` is not current-attempt
  proof.
- **High — six-file recovery is preservation-first and requires a fresh output
  root for any later diagnostic retry:** before cleanup, restoration, or retry,
  preserve all six finals; every matching run-token temp/backup; lock/owner;
  both manifests and both Step `08` inputs; selected Rscript/R program and
  package/startup state; unrelated bytes; producer/validator/wrapper streams;
  scheduler job/accounting/logs; checkout/submit CWD; and environment
  overrides. Summary visibility precedes final validation/hash commit and is
  not successful-attempt proof. An incomplete restore deliberately retains
  the owned lock and surviving backup while one final may be absent. Do not
  combine attempts, manufacture a member or summary, remove a foreign or
  retained recovery lock, discard a backup, trust names/hashes/timestamps or
  stale wrapper success, or retry the same output root. Rule out every Step
  `09` writer and Step `09c`/artifact/report reader first; any separately
  authorized nonproduction diagnostic retry uses one isolated absolute output
  root with the same explicit inputs.
- **Medium — pairing, statistical, validation, and artifact language must stay
  exact:** the manifest alone supplies identical control/treatment replicate
  labels and at least two paired strata. Step `09` publishes CMH-ranked cohort
  candidates under `legacy_provisional_v1`, not validated RNA-editing sites,
  strand/sense interpretation, completed scientific review, or biological
  readiness. Producer exit `0` proves current local checks/publication but not
  R program/runtime/package or durable attempt identity; the summary omits
  sibling hashes. Validator exit `0` may contain failed rows and does not
  independently recompute table estimability, CMH statistic, p-value, or
  common odds ratio even though its expected text says CMH was recomputed.
  Fake-R passes remain orchestration fixtures; guarded-real-R and independent-
  oracle passes remain distinct local semantic fixtures; scheduler and
  artifact success do not promote evidence state. Artifact change is only the
  reviewed final producer path/hash, never an identity, schema, ordering,
  consumer, or scientific-meaning change.
- **Accepted findability, commands, documentation, and rollback:** add one
  adjacent owner README routing root/arbitrary-CWD shell and validator,
  sibling/direct/guarded R, independent oracle, checkout-root scheduler,
  manifests/Step `08`/R/output/lock/summary choices, focused tests, recovery,
  artifact provenance, evidence limits, and next safe action. Correct the
  contract's unimplemented/flat-path wording and update every impact-directed
  architecture, inventory, test, ownership, runbook, troubleshooting,
  predecessor/consumer, artifact, local-R, roadmap/handoff, and lifecycle route
  only at migration close. Add no diagram because semantic identity, DAG
  edges, and public flow do not change. Focused final commands use the owner-
  local shell, validator, guarded-R runner, independent oracle, and central
  Step `09` scheduler selection; the exact complete card-boundary gate remains
  `RSCRIPT_BIN=/usr/local/bin/Rscript make -s all-checks`. Revert
  documentation first, atomic cutover second, then scheduler, validator,
  signal/concurrency, publication-order, and runtime/input checkpoints in
  reverse. Git rollback never authenticates, recovers, deletes, or alters
  runtime outputs, locks, backups, R state, or scheduler logs, and no
  compatibility surface is justified.
- **Evidence boundary:** this was a separate committed-time read-only pass by
  the same campaign agent; independent authorship is not claimed. No source,
  test, harness, dependency, R runtime/package, scheduler, cluster, production,
  scientific-review, provisional-policy, editing-site, or biological evidence
  changed or ran.
- **Card-boundary gate:** `git diff --check` passes and the exact RUNBOOK
  documentation validator reports `PASS documentation structure (208 Markdown
  documents, 129 task cards, 6 Mermaid sources)`. No usability-review path,
  lifecycle, dependency, cycle, orphan, schema, anchor, or diagram finding
  remains.
