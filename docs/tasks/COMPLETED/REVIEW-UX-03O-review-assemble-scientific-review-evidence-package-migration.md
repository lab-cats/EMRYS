# REVIEW-UX-03O — Review scientific-review package migration usability

## Objective

Review `MIG-03O` for operator, maintainer, automation, recovery, evidence-
governance, provenance, and scientific-language continuity across every
explicit Step `09c` path change.

## Why this exists

The migration changes Python, shell, test/fixture, example/schema, imported-
contract, artifact, run-summary, report, Make, and coverage paths for a
thirteen-file scientific-review transaction. Correct relocation can still
leave stale commands, hidden upstream/evidence/output/lock/summary choices,
unsafe retry guidance, status overclaim, or an undiscoverable final owner.

## Fixed decisions

- Review only; do not redesign arguments, schemas, categories, scientific
  states, decisions, transaction, artifact/report meaning, or evidence policy.
- Preserve explicit repository-relative and absolute-path invocation without
  installation, ambient import discovery, global `sys.path`, or a legacy alias.
- Keep synthetic migration fixtures distinct from production inputs, completed
  scientific review, cluster proof, validated editing sites, or biological
  readiness.

## Blocked by

- [REVIEW-REL-03O](../COMPLETED/REVIEW-REL-03O-review-assemble-scientific-review-evidence-package-migration.md) — Required: completed reliability review supplies the corrected obligations.

## Completion unblocks

- [MIG-03O](../TODO/MIG-03O-migrate-assemble-scientific-review-evidence-package-owner.md) — Fully: migration selection may begin after all three reviews close.

## Prerequisites

- Inspect the committed reliability-reviewed cards against public shell/Python
  CLI, arbitrary-CWD, Make, examples/schemas, import consumers, artifact/run-
  summary/report paths, evidence language, recovery, and rollback journeys.

## Required context

- `MIG-03O`; Step `09c` runbook/troubleshooting commands; shell/Python help;
  public CLI and Make characterization; candidate example/schema routes;
  migrated predecessor loaders; artifact/run-summary/report consumers;
  coverage; owner contract; current/future topology; manifests; Step `08`/`09`/
  review/evidence/output/lock/summary diagnostics; scientific-state language;
  and the thirteen-file transaction evidence boundary.

## Questions owned by this card

- None.

## In scope

- Root/arbitrary-CWD shell and direct-Python dry-run/execute/repeat journeys;
  Python/executable, manifest, Step `08`/`09`, review plan, evidence manifest/
  payload, example/schema, output, scratch, lock, and summary selection;
  provisional science-state and non-biological wording; publication and
  restoration failure, mutation, relative-path disagreement, residue, and
  safe preservation; Make/test/import/artifact/run-summary/report commands;
  provenance; owner findability; links; rollback; and next-safe-action
  instructions.

## Out of scope

- New aliases/wrappers, package installation, PATH/`PYTHONPATH` redesign,
  schema/state/policy changes, transaction/recovery redesign, artifact/report
  redesign, scheduler/cluster/production work, completed scientific review,
  biological interpretation, or future units.

## Deliverables

- Journey-based findings with exact card/documentation corrections and dated
  audit dispositions.

## Acceptance evidence

- Every supported healthy/failure transition has one final command, owned
  diagnostic, artifact expectation, preservation route, and evidence ceiling.
- The owner README and runbook make shell/Python, distinct dry-run effects,
  example/schema and upstream/evidence/output/lock/summary choices, restoration
  residue, focused tests, import/artifact/run-summary/report provenance,
  evidence state, and rollback discoverable without an alias or overclaim.

## Canonical documentation updates

- This card, `MIG-03O`, current roadmap/handoff where status changes, and the
  dated audit log.

## Escalation conditions

- Stop if continuity requires a legacy path, changed public interface,
  dependency installation, public package identity, schema/state/scientific/
  biological claim, or an unreviewed alias/package contract.

## Completion record

Completed against clean, published, local/upstream/live-remote-equal selection
checkpoint `35ba58384bf0fae40f530869a5a0c432c69350bd`.

- **High — the supported shell and Python journeys need one exact final owner
  and explicit choices:** at migration close, root shell use invokes the
  mode-`0755`
  `src/norad/evidence/assemble_scientific_review_evidence_package/step_09c_scientific_validation.sh`;
  direct Python use invokes the mode-`0644` sibling through the selected
  project interpreter. From another CWD, make the launcher or Python file,
  interpreter, sample/partition manifests, all three Step `08` inputs, Step
  `09` analysis directory, review plan, evidence manifest and every payload,
  and output root absolute. The fifteen retained `configs/` examples/schemas
  are structural references, never discovered production evidence. Use one
  unique review ID and a fresh absolute output root for the safest first run.
  No installed command, package import, legacy alias, wrapper, symlink,
  ambient `PYTHONPATH`, or global `sys.path` route is supported.
- **High — dry-run, execute, replacement, and consumption remain distinct:**
  omit `--execute` to validate all declared inputs and print resolved choices
  without creating the output root, lock, scratch, backup, or final files.
  Execute publishes exactly twelve payloads and then the summary under
  `<output-root>/<review-id>/`; an existing transaction is replaceable only
  when all thirteen members are present. A visible summary is not committed-
  attempt proof because final validation, hashes, and the second 32-input
  check follow publication, and the summary omits sibling hashes. Artifact
  indexing treats the final Python file as producer and
  `step09c_review_summary_v1` as the failure marker; run-summary reconstructs
  and validates the full explicitly selected package; reporting consumes only
  that authorized normalized record. None of those consumers proves the
  runtime attempt, production execution, completed review, cluster evidence,
  validated editing sites, or biological readiness.
- **High — thirteen-file failure and signal recovery is preservation-first:**
  before diagnosis, cleanup, restoration, or retry, preserve all thirteen
  finals; every matching `.step09c.<run-token>.tmp`, `.previous`, and
  `.RECOVERY.txt` path; the `.step09c.lock`; all 32 inputs; interpreter/code
  identity where independently available; streams, environment, PID/signal
  evidence; and unrelated bytes. Never delete a foreign or recovery lock,
  discard a backup, combine attempts, manufacture a member/summary, or infer a
  retry from names, summary visibility, or cleanup. Incomplete restore may
  retain one absent final plus its backup, the other twelve restored finals,
  lock, empty transaction directory, backup directory, and notice. A `TERM`
  after summary can retain thirteen unvalidated new finals, thirteen backups,
  lock, and empty transaction directory without a notice. A
  `KeyboardInterrupt` after summary can leave new finals while deleting the
  backups, transaction directory, and lock. These severe defects are
  characterized, not approved. Rule out every writer and Step `09c`/artifact/
  run-summary/report reader before any separately authorized diagnostic retry;
  use a new isolated absolute output root rather than the ambiguous root.
- **Medium — exact language and next action stay provisional:** this owner
  validates and packages declared evidence; it does not rerun CMH, infer
  reviewer decisions, authenticate declared Git/software/runtime metadata,
  complete a production scientific review, or establish biological truth.
  `science_review_complete_exploratory` remains provisional and
  `biological_interpretation_ready` remains rejected. Synthetic fixture,
  artifact, run-summary, report, and local coverage passes must be reported
  separately from production, scheduler, cluster, scientific-review, and
  biological evidence. When an input/declaration fails, correct the owned
  source or declaration and dry-run again; never edit a hash, row count,
  category, status, or decision merely to force acceptance.
- **Accepted findability, commands, documentation, and rollback:** add one
  adjacent owner README at migration close. It must route final shell/Python
  dry-run and execute commands, absolute-CWD use, retained config/schema
  references, every explicit upstream/evidence/output/lock/summary choice,
  thirteen-file preservation, private-loader/artifact/run-summary/report
  provenance, and the evidence ceiling. Focused final checks are the moved
  direct shell and Python tests, migrated Step `08`/`09` loader tests,
  artifact-adapter/run-summary, independent-golden, and public-CLI suites. The
  exact complete card-boundary gate remains
  `RSCRIPT_BIN=/usr/local/bin/Rscript make -s all-checks`; documentation close
  separately runs `make -s documentation-check`. Revert documentation first,
  atomic cutover second, then signals/concurrency, recovery, publication,
  input, and wrapper checkpoints in reverse. Git rollback changes repository
  files only; it never authenticates, restores, removes, or alters runtime
  evidence, outputs, locks, backups, or review state.
- **Evidence boundary:** this was a separate committed-time read-only pass by
  the same campaign agent; independent authorship is not claimed. No source,
  test, fixture, configuration, dependency, runtime, scheduler, cluster,
  production, scientific-review, or biological file changed or ran.
- **Card-boundary gate:** `git diff --check` passes and the exact RUNBOOK
  documentation validator reports `PASS documentation structure (213 Markdown
  documents, 133 task cards, 6 Mermaid sources)`. No usability-review path,
  lifecycle, dependency, cycle, orphan, schema, anchor, or diagram finding
  remains.
