# REDUCE-01 repository audit

This is a repository-wide finding record for [REDUCE-01](backlog_matrix.md#maintainability-and-release). The backlog matrix remains the authority for status and acceptance. Finding IDs below are navigation aids, not another task-status list or permission to implement a proposal.

## Snapshot and measurement

The audited commit is `3a672fdf8e55b30efc63dea9aecc4a29d28a5f4d`. Its product source, tests, scripts, and configuration are unchanged from `42d02c5a`; the intervening commit changes documentation. The provisional comparison is the **local** merge base `f2c0149e73a685b6f3d5b162f3804edee7c78c10`. Live pull-request base and remote freshness were unavailable during the source audit.

The snapshot contains 575 tracked files. Under the repository's existing physical-line convention—tracked product `.py`, `.R`, `.sh`, `.css`, `.j2`, and `Snakefile`, excluding generated `renv/activate.R`—204 product files contain 65,280 lines. The integration diff changes 54 of those files; this audit also examines older, unchanged owners.

| Surface in provisional diff | Added | Deleted | Accounting boundary |
| --- | ---: | ---: | --- |
| Product code | 11,885 | 3,169 | Only this surface can establish product-code reduction. |
| Tests and fixtures | 22,030 | 2,751 | Protection and evidence value require separate review. |
| Markdown and diagrams | 7,038 | 586 | Documentation deletion cannot offset product growth. |
| Schemas | 159 | 88 | Contract and compatibility decisions remain separate. |
| Repository scripts | 69 | 60 | Tooling is reported separately. |
| Configuration and governance | 510 | 257 | Includes product resource configuration. |
| Other tracked files | 1 | 0 | One `Projects/.gitkeep`; no product saving. |

The categories above assign Markdown/diagrams before tests and sum to 41,692 additions and 6,911 deletions across 168 changed files. Orchestration alone accounts for 10,361 added and 2,692 deleted product lines in the provisional diff. The 25% target is **not measured as achieved**. The accepted row has not selected comparison refs, counted surfaces, or gross additions versus net growth. If these provisional refs and product code are selected, a gross-additions interpretation requires at least 2,972 fewer added product lines; a net-growth interpretation requires at least 2,179 fewer product lines of growth. Retained evidence has not been proposed for deletion or counted as savings.

The whole-repository stock extends beyond that diff: orchestration has 26 product files/28,108 lines, reporting 47/12,274, contracts 22/6,984, stages 36/5,063, libraries 33/4,574, analyses 12/3,781, and evidence 20/3,165. The 54 changed product files contain 37,993 current lines; 150 unchanged product files contain 27,287 lines (41.8% of current product code). Some large unchanged owners still need fine-grained review, including scientific-context projections and figures, Step 08/09 contracts, and storage qualification. Their size alone proves no reducible behavior. Coordinate size exceptions with `SIZE-01`; this audit must also inspect their callers and protections.

Evidence here is committed-source review, existing test/contract inspection, and the isolated probes identified below. Repository tests, hosted CI, installed interactive Watch, Slurm, Viking, scientific review, and biological interpretation were not performed for this audit.

## Findings matrix

| ID | Finding | Classification | Discovery and protected boundary | Reduction evidence and next check |
| --- | --- | --- | --- | --- |
| F01 | Measurement definition | Undecided | The [backlog row](backlog_matrix.md#maintainability-and-release) leaves refs, surfaces, and gross versus net additions open. A whole-repository audit includes code that predates the integration diff. | Select the metric before implementing or claiming progress. Report product, tests, tooling, schemas, configuration, documentation, and retained evidence separately. |
| F02 | Step 09/10 R validators | Consolidation candidate | [Step 09](../../src/emrys/analyses/paired_cmh_candidate_ranking/step_09_cmh_common.R) lines 108–122 and [Step 10](../../src/emrys/analyses/paired_cmh_candidate_ranking/scientific_context_projection/scientific_context_projection.R) lines 131–145 define identical safe-ID and hash validators. Both already load [`input_contract.R`](../../src/emrys/libraries/input_contract.R). Step 08's similar check lacks the same scalar guard. | Moving the two exact definitions to the existing shared R owner could save about 15 product lines, subject to an exact diff and diagnostic parity. Do not include Step 08 without equivalence proof. |
| F03 | Dashboard sample initialization | Consolidation candidate | [`dashboard.py`](../../src/emrys/orchestration/run_coordinator/dashboard.py) lines 1097–1114 register samples on the wildcard event. Lines 1137–1143 and 1167–1175 repeat initialization. Current parser dataflow gives those later branches no path to a new sample. | An isolated 120-order event probe reached normal initialization 24 times and neither fallback. This is not exhaustive validation. Test active, completed, missing, repeated, and reordered log events before removing either branch; potential saving is about 16 product lines. |
| F04 | Dashboard stage placeholder | Representation cleanup | All 16 `STAGES` rows in [`dashboard.py`](../../src/emrys/orchestration/run_coordinator/dashboard.py) lines 26–204 contain an unused third `None`; tuple consumers and one test assume six fields. | Removing it could delete about 16 physical lines but offers little behavioral simplification. It must not stand in for a meaningful 25% reduction. |
| F05 | Inspection and Watch overlap | Preserve; architecture undecided | [`control.py`](../../src/emrys/orchestration/run_coordinator/control.py) calls installed Watch for raw jobs, submissions, and Runs. [Inspection presentation](../../src/emrys/orchestration/run_coordinator/_inspection_presentation.py) owns terminal interaction and fresh action handoff; [dashboard](../../src/emrys/orchestration/run_coordinator/dashboard.py) supplies diagnostic parsing/rendering; [inspection](../../src/emrys/orchestration/run_coordinator/inspection.py) admits Run completion and recovery. The [coordinator contract](../../src/emrys/orchestration/run_coordinator/CONTRACT.md) lines 565–657 separates these authorities. | No whole-file deletion or unified-model saving is established. Map every selection, plain/interactive view, stream, action, and fault caller before a measured prototype. |
| F06 | Interactive log regex | Potential responsiveness defect | [Inspection presentation](../../src/emrys/orchestration/run_coordinator/_inspection_presentation.py) lines 1021–1044 applies an arbitrary user regex during interactive painting and search submission. The tail has byte and line limits, but regex runtime has no bound. The [Runbook](../operations/RUNBOOK.md) lines 96–103 promises regex search. | An isolated standard-library pattern against a 30-character line exceeded a 0.5-second subprocess limit. Rich was unavailable, so the Watch path was not reproduced. A separate repair must preserve regex behavior and navigation; book no REDUCE saving. |
| F07 | Step 10 shell worker | Preserve pending proof | The 192-line [Step 10 worker](../../src/emrys/analyses/paired_cmh_candidate_ranking/scientific_context_projection/scientific_context_projection.sh) checks input hashes and staged receipt payloads before successful exit. The runner owns publication, but Step 10's grouped Python validation occurs **after** native publication ([`task.py`](../../src/emrys/orchestration/run_coordinator/task.py), lines 1742–1758 and 2732–2782). | Retiring the shell would remove a distinct prepublication check unless an equal or stronger check is placed at that boundary. Planner, package roster, direct command, tests, and owner documentation would also need caller-complete migration. No safe saving is measured. |
| F08 | Reporting transaction publishers | Preserve; prototype undecided | Artifact-index and HTML publishers already share file, signal, and exclusive-publication helpers. Their directory modes, source rechecks, payload validation, receipt ordering, rollback anchors, and interrupt behavior differ ([artifact-index publisher](../../src/emrys/reporting/_artifact_index/publication.py); [HTML publisher](../../src/emrys/reporting/_run_report/publication.py)). | The proposed common engine has no demonstrated net reduction. Preserve independent fault coverage; count savings only after one prototype replaces both loops without adding a parallel policy layer. |
| F09 | Replace-in-place publishers | Defective recovery boundaries; separate repair | [Validation publication](../../src/emrys/libraries/validation/publication.py) and [reference-provenance reconciliation](../../src/emrys/evidence/reference_provenance/reconciler.py) share a broad stage/backup/restore shape, but publish one versus three outputs. Their owner guidance records ownership or restoration gaps. | A shared primitive requires fault and ownership proof after recovery behavior is settled. These are not safe deletion estimates; preserve backups, partials, locks, and recovery evidence. |
| F10 | Resource benchmark recommendation | Source-level defect; tooling | [Benchmark script](../../scripts/benchmark_stage_resources.py) lines 322–365 discards failed trials before recommending a value. A value with one passing and one failing repetition can therefore be recommended, although `run` returns nonzero. The current summary test lacks that mixed case. [`SETUP-02`](backlog_matrix.md#deferred-operational-work) requires rejecting any failed repetition while the helper remains. | Add a mixed-repetition characterization before a separately selected repair. Later coordinated retirement is already owned by SETUP-02. Neither action supplies product-code savings; retain raw trial evidence. |
| F11 | Root `.env.example` | Stale configuration candidate | [`.env.example`](../../.env.example) instructs copying seven variables with no other tracked reader. The current [loader](../../src/emrys/orchestration/run_coordinator/onboarding.py) lines 106–109 and 153–190 accepts a versioned `.env` with Projects root, site, and optional log root; it ignores a copied example lacking the version marker. | Confirm external operator use, then decide whether to retire the stale file or replace it with a current example. Eleven configuration lines; zero product-code saving. Keep `.env` ignored. |
| F12 | Viking resource YAML duplication | Undecided; preserve | The resource block in [the Viking profile](../../configs/execution_profile.csu_viking_ev_pum1.yaml) lines 5–43 matches the packaged [default](../../src/emrys/orchestration/run_coordinator/resources/default_execution.yaml). Explicit fields bind source provenance and affect resume inheritance ([`execution_profile.py`](../../src/emrys/orchestration/run_coordinator/execution_profile.py), lines 416–444). | The 39 repeated configuration lines are not a safe mechanical deletion. Compare new-Run and resume identity, hashes, and accepted site policy before a decision; zero product saving is established. |
| F13 | Narrow coordinator mechanics | Candidate; low yield | Explicit terminal confirmation and token-bound Slurm schema lists repeat small mechanics across coordinator owners. Request-record publication and recorded scheduler streams already have single or shared owners; directory synchronization wrappers enforce different trust and recovery checks. | Audit each complete caller set and fault behavior before sharing anything. Current source estimates are single-digit product lines, not evidence of a 25% path. |
| F14 | Scientific and evidence lookalikes | Preserve | Step 08/09 parsers accept different schemas and scientific policies. Step 09 computation and independent validation, Step 10 reference-window re-derivation, and the Snakefile's static owner fence detect distinct failures. | Do not count independent oracles, cross-checks, or different admission boundaries as duplication. Changes require their own scientific/contract review and equal-strength protection. |
| F15 | Documentation, tests, CI, and formatting | Separate owner review | Documentation structure checks, their failure-injection tests, CI preflight, synthetic E2E, scientific oracles, and retained measurements cover different failures or evidence levels. `SHFMT_BIN` in [`make_quality.mk`](../../scripts/make_quality.mk) has no invocation. | Review gate value under `ASSURANCE-01`, prose under `DOCS-01`, and surviving scripts under `OPS-03`. Removing the unused Make setting would save one tooling line; none of these changes counts as product reduction. |
| F16 | Artifact-index API facade | Retirement candidate | The 30-line private [`_artifact_index/api.py`](../../src/emrys/reporting/_artifact_index/api.py) only re-exports sibling symbols. No tracked static caller was found at this commit; independent contract goldens import the underlying modules. | Confirm dynamic imports, packaged API expectations, and external use before deleting. Gross saving is 30 product lines and one file; net effect needs an exact diff. |
| F17 | Run-summary `transaction.py` | Owner consolidation candidate | The 47-line [`_run_summary/transaction.py`](../../src/emrys/reporting/_run_summary/transaction.py) contains deterministic value helpers, no transaction. Only same-owner document/projection modules import it. | Inline or move helpers within that owner, then check summary JSON/TSV, ordering, schema, and independent goldens. Estimated net saving 10–25 product lines and one file, subject to a prototype. |
| F18 | Report previous-output snapshots | State simplification candidate | Frozen `ReportContext` retains a mutable `previous_snapshots` mapping, but publication uses only its truthiness; the disk is separately rechecked before output. | A boolean may avoid retained mutable state while preserving complete-output refusal. Verify every context construction and publication fault case; no material line saving is established. |
| F19 | Strict JSON admission | Consolidation candidate; undecided | [Orchestration contracts](../../src/emrys/contracts/orchestration/api.py) and [artifact contracts](../../src/emrys/contracts/artifacts/_artifact_contracts/schema.py) each reject duplicate keys and nonstandard numbers. Their schema registries have different ID, caching, error, and diagnostic policies. | Compare complete callers and diagnostics before sharing the narrow parse policy; do not merge registries by appearance. Net product saving is unmeasured. |
| F20 | Storage receipt JSON | Potential admission defect; separate review | [Storage qualification](../../src/emrys/evidence/storage_inventory/qualification.py) uses bare `json.loads` for direct and two-phase receipts. Duplicate keys are last-wins and `NaN` is accepted in an isolated parser probe; current tests cover malformed syntax/non-objects only. | Characterize duplicate/nonstandard values through actual receipt admission, preserve raw-byte hashes and direct versus cross-node identity policy, then choose a repair. No bypass or REDUCE saving is established. |
| F21 | Stage BAM checks | Preserve distinct policy | [Canonical BAM](../../src/emrys/stages/canonical_bam/step_02_sort_index_bam.sh) and [Split N Cigar](../../src/emrys/stages/split_n_cigar/step_05_split_n_cigar_reads.sh) both check a BAM/index pair, but the former requires `LB`/`PL` and decides whether input bytes can be reused; the latter checks a narrower post-GATK contract. | Do not unify their checks without matching decision inputs, diagnostics, timing, and tests. No net saving is established. |
| F22 | Shell worker argument switches | Consolidation candidate; low yield | Ten shell owners share [`argument_parsing.sh`](../../src/emrys/libraries/argument_parsing.sh) but repeat 83 option-dispatch arms. Required/optional options, defaults, diagnostics, and published help differ. | Prototype a caller-complete roster parser only if it reduces total code while preserving each CLI and shell fault check. Gross repeated arms are not a net saving. |
| F23 | Strict TSV streaming readers | Consolidation candidate | [Step 09](../../src/emrys/contracts/scientific_evidence/step09.py) and [scientific context](../../src/emrys/contracts/scientific_evidence/scientific_context.py) repeat a bounded UTF-8/strict-CSV row stream. Their dynamic versus fixed header policies differ; the existing shared strict TSV reader materializes rows. | A streaming helper in the existing validation TSV owner might save 15–25 net product lines. Preserve header/error order and bounded memory with a prototype and direct tests. |
| F24 | Scientific R dead guards | Small dead-code candidates | [Step 10 R](../../src/emrys/analyses/paired_cmh_candidate_ranking/scientific_context_projection/scientific_context_projection.R) repeats a five-output collision check after checking the full nine-output set, and passes an unused `catalog` parameter; Step 09 filters an already equal replicate set. | Verify diagnostics and edge cases, then remove only the unreachable checks in an approved scientific slice. About 10 lines for the Step 10 guard; other savings are smaller. |
| F25 | Unproduced report CSS | Output-change candidate | About 42 lines of [`run_report.css`](../../src/emrys/reporting/styles/run_report.css) style `.panel-grid`, `.status-panel`, `.status-value`, and `.status-*` classes with no producer in audited source. | Confirm all renderer/plugin paths. Deletion changes newly published report bytes and recorded CSS hash, so it needs an explicit output/provenance decision; no evidence is deleted. |
| F26 | Paired-figure palette | Consolidation candidate | [Scientific figures](../../src/emrys/reporting/paired_cmh_candidate_ranking_report/figures.py) and [context figures](../../src/emrys/reporting/paired_cmh_candidate_ranking_report/scientific_context_figures.py) use the same eight-color tuple by pair index. | Import one palette from the existing owner and verify deterministic SVG hashes; about 9 net product lines are possible. Other plotting and legend behavior differs. |
| F27 | Figure asset construction | State/owner simplification candidate | [`figure_models.py`](../../src/emrys/reporting/paired_cmh_candidate_ranking_report/figure_models.py) constructs the same single-asset panel in validation and `assets`. | A single validated projection may reduce about 15 lines, pending a diff and duplicate-ID, mixed-provenance, and hash/size parity tests. |
| F28 | Specialist manifest drafting route | Public-surface decision; preserve pending review | [`emrys init manifests`](../../src/emrys/orchestration/run_coordinator/onboarding.py) publishes standalone drafts using a core also used by guided Project Init. The standalone route is documented and tested and predates the provisional integration diff. | Retirement could remove roughly 80–90 current product lines only after an explicit operator/contract decision; it saves zero gross additions against the provisional refs. |
| F29 | Large coordinator owners | Preserve distinct boundaries | Stop, Doctor repair, and [task materialization](../../src/emrys/orchestration/run_coordinator/materialization.py) appear large, but scheduler transport, retained Run admission, repair claims, and ordered scientific commands serve different decisions. | No caller-complete large deletion was found. Continue tracing high-growth branches and fault tests; line count or a moved table is not a saving. |
| F30 | Generic manifest validator | Public-surface decision; preserve pending review | The 190-line [manifest validator](../../src/emrys/ingestion/sample_manifest_admission/validator.py) supports optional `replicate`, unlike Project Init's Step 08 contract. It has a public CLI, two Make checks, owner tests, and operator documentation. | Retire only after a caller-complete compatibility decision. Gross current product surface is 190 lines/one file, but it predates the provisional diff and saves zero gross additions. |
| F31 | Derivable workflow-profile fields | Schema/config decision | The [profile](../../src/emrys/workflow/contracts/local_cmh_v2.json) repeats 69 selectors derivable from scope type and two 12-key owner lists validated against tasks. Readers use these fields in materialization, workflow, and immutable plan identity. | Coordinate with `PROFILE-CONTRACT-01`/`SCHEMA-01`; no mechanical deletion or product saving. Preserve profile hashes, retained Runs, identity, and recovery. |
| F32 | Runtime binding rechecks | Preserve distinct trust times | [Runtime inspection](../../src/emrys/evidence/runtime_availability/inspector.py) hashes current observed targets and seals, while [lifecycle admission](../../src/emrys/orchestration/run_coordinator/lifecycle.py) rechecks serialized Attempt identities at execution. | Similar path/hash checks serve different authority moments. No caller-complete reduction is established; separate process-count optimization from code compression. |
| F33 | Step 07 region-validation limits | Known evidence gap; preserve producer guard | [Shared contig parsing](../../src/emrys/libraries/references/contigs.py) is intentionally permissive, so [Step 07's producer](../../src/emrys/stages/partitioned_cohort_mpileup/producer.py) rejects invalid FAI contigs itself. The [validator](../../src/emrys/stages/partitioned_cohort_mpileup/validator.py) has tested region-file false-pass/false-fail limits. | Do not delete the producer guard as duplication. Repairing validator limits needs separate scientific/evidence review; no REDUCE saving is established. |

## Source and protection details

### F01 — Reproducible measurement

The provisional stock uses `git ls-tree -r --name-only 3a672fdf` and physical lines of the tracked product extensions under `src/`, excluding generated `renv/activate.R`. The provisional diff uses `git diff --numstat --no-renames f2c0149e 3a672fdf`; classification gives Markdown/diagrams precedence over tests. Product stock grew from 201 files/56,564 lines to 204 files/65,280 lines. Three new product files are `_inspection_presentation.py`, `_submission_inspection.py`, and `scheduler_observation.py`; no product file was retired. Product gross additions are 11,885 and net growth is 8,716. A 25% reduction of gross additions would cap additions at 8,913; a 25% reduction of net growth would cap growth at 6,537. Deleting unchanged baseline code lowers net growth but does not lower gross added lines. Several whole-repository candidates below, including F16/F17 and F23–F27, are in unchanged baseline files. These are conditional thresholds, not achieved savings. Ref selection, line moves, non-product surfaces, and the exact meaning of “additions” still require an accepted decision before implementation or scorekeeping.

### F02 — Shared R validators

Step 09 and Step 10 each define the same `validate_safe_id` and `validate_hash` bodies. Their entrypoints load the existing `libraries/input_contract.R` before these definitions. Moving the two definitions there would remove about 30 duplicated lines and add about 15 shared lines, for a provisional net saving of 15 product lines. Step 09 validates Step 08 receipt hashes; Step 10 checks six supplied hashes. Step 08's similar safe-ID check lacks the scalar-length guard and is outside this candidate. A caller-complete change needs exact invalid scalar, character, and hash diagnostic parity plus the real-R producer suites. No such change or test has run.

### F03 — Parser reachability

A job starts with an empty wildcard string. The wildcard event is the only assignment of a nonempty wildcard string and registers its sample immediately. Completion and final active-job processing read that same value; `samples` is never cleared. This makes the two later initialization blocks unreachable under the current parser, including the event-order permutations probed locally. Before removal, assert sample order and history for malformed and repeated events, and retain the contract's distinction between log observations and verified Run progress.

### F04 — Stage-table placeholder

Every `STAGES` row has a third `None`, while tuple consumers and a shape test expect the six-field representation. Removing the placeholder would also require updating that expectation. This is a representation cleanup with no demonstrated reduction in decisions, branches, or owned behavior; its 16 physical lines must remain separate from meaningful product compression.

### F05 — Installed Watch and inspection

At the audited commit, `control.watch_from_args` (lines 3009–3075) fixes a Project Run, associated request, or raw scheduler selection and reuses `inspect_from_args` (2632–2801). All modes enter the single terminal owner in `_inspection_presentation.watch` (1069–1368). Its `WatchSnapshot` (396–417) keeps request, application, Run, scheduler, and trace observations separately dated. Explicit refresh re-admits the selected request/application/Run; timer refresh reads only scheduler and diagnostic streams (538–705, 1172–1194). Raw scheduler mode cannot acquire Project/Run action authority; Run-only mode reports scheduler identity unknown. Static Inspect and Watch both use `project_run`; Watch also uses the live dashboard parser, stream cache, and renderer. Action callbacks carry exact selectors into newly parsed CLI handlers after terminal restoration. Direct tests cover fixed selection, read-free painting, stale-evidence clearing, raw-mode separation, view parity, and action handoff. `DASHBOARD-RETIRE-01` retains this shared dashboard code. No whole-file retirement is established. Any redesign must show parity for static, Project Run, associated-request, raw/offline, interactive, and nonterminal modes before a measured saving is claimed.

### F06 — Regex cost

A query-length cap alone would not solve catastrophic backtracking: a short pattern can be slow against a short allowed log line. The audit has not chosen a dependency or changed the documented regex feature. Exact interactive behavior remains unverified.

### F07 — Step 10 check order

The shell does not own locking, final links, or rollback; the runner does. Its `validate_receipt_payloads` checks hashes and row counts for all four staged outputs before producer success (`scientific_context_projection.sh`, lines 80–139). The runner's prepublication validation set contains only Steps 08 and 09 (`task.py`, 1742–1758); Step 10's grouped validator runs after native publication (2732–2782). The coordinator contract distinguishes producer abort before publication from failed validation after commit (lines 978–988 and 1117–1131). The shell test checks a valid fake receipt and one broken receipt, not each payload mismatch. Retiring the shell requires prepublication parity for all payloads, input hashes, receipt shape, planner/direct callers, and recovery/abort fault paths. No safe 192-line saving is established.

### F08 — Reporting transactions

The coordinator has one production dispatch for each reporting publisher. The artifact-index publisher descriptor-walks a destination below its admitted artifact root, rechecks native sources and package, stages TSV/QC/JSON, then links the JSON manifest last. The HTML publisher can create independent output directories, validates staged and final scientific/evidence HTML, and links a TSV receipt last. Their rollback, lock, and anchor handling differ. The existing `_files.py`, `_signals.py`, and `exclusive_publication.py` already share equivalent low-level mechanics. Direct reporting tests exercise mid-publish failures and retained recovery state. No measured common engine is shorter or equal in protection; independent tests and receipt ordering remain necessary.

### F09 — Replace-in-place recovery

The validation library's one-file publisher serves four validator owners through `validation/runtime.py` (lines 27–49). It validates the staged and predecessor reports, then replaces the final (`validation/publication.py`, 13–70). Existing characterization tests show a late foreign final can be deleted during rollback and a failed restore can leave the predecessor backup while releasing the lock. Reference-provenance reconciliation publishes three outputs and moves all predecessors before entering its rollback handler (`reconciler.py`, 83–126). Its failed-restore test leaves backups with the lock removed; the owner README also records partial backup-move and diagnostic-loss risks. `exclusive_publication.publish_exclusive` covers absent or exactly expected single-file bytes, not this complete replacement policy. Repair and consolidation need separate ownership/fault review. The 58-line validation publisher is gross exposed surface, not a booked net saving, and none of its failure tests are deletion offsets.

### F10 — Mixed benchmark trials

`_write_summary` groups passing rows only, computes a median for those rows, and can recommend the value (script lines 322–366). `run` retains each failed row and returns 1 if any trial failed (477–510). The full command therefore reports failure while `summary.tsv` may still say `recommended=yes` for a partly failed value. Both statements can reach an operator. Existing tests cover all-pass and all-fail values, not mixed repetitions. A focused case needs to assert raw-row retention, nonzero exit, and no recommendation for that value. This is a separate tooling repair, with no product-code saving.

### F11 — Copied configuration

The example's instruction leads to a `.env` lacking `EMRYS_ENV_VERSION=1`. The current loader skips an unmarked file (`onboarding.py`, 153–170), so the example's promised settings do not take effect through EMRYS's saved-setting path. `emrys setup` then refuses to create new settings if that file exists (215–220). The seven old variable names and `.env.example` path have no other tracked readers or references in the audited source. This is a source-derived operator trap, not a reproduced operator session; external scripts remain unverified consumers. Retirement or replacement belongs to configuration and onboarding review, with zero product-code saving.

### F12 — Resource values and provenance

The Viking profile's resource values match the packaged defaults at the audited commit, and a test checks declaration equality. The provisional base had a different fixed Viking resource policy, so this equality is an integration change. The current test does not compare source provenance. `load_execution_profile` gives explicit resource fields the selected source path and SHA-256 and sets `computational_resources_explicit=True` (416–444). A placement-only profile instead inherits the predecessor's resource policy on resume (`control.py`, 695–704 and 843–858). The raw profile source hash also enters binding. Removing the repeated YAML block could change new-Run and resume identity even if the values remain equal. The 39-line configuration candidate needs a paired identity/provenance/resume comparison; it cannot count as product savings.

### F13 — Small coordinator repeats

Three `y`/`yes` confirmation helpers in `control.py`, `doctor.py`, and `onboarding.py` repeat a small decision, although prompt and diagnostic ownership may differ. `_submission_inspection.py` hardcodes the same token-bound v2–v4 version set represented by `slurm_submission._REQUEST_SCHEMAS[1:]`; the trust and compatibility boundary needs comparison before sharing it. `request.json` has one writer, and scheduler submit/stop already share transcript publication. Directory-sync helpers vary in no-follow, identity, type, and failure semantics; sharing their surface shape could lose protection. These candidates offer only single-digit product-line savings unless a measured prototype proves otherwise.

### F14 — Scientific and evidence independence

Step 08 admission validates site rows, scope counts, sample DP/AD/AF, and carried location (`contracts/scientific_evidence/step08.py`, 483–555). Step 09 additionally binds analysis identity and exact carry-forward of Step 08 metadata/sample fields (`step09.py`, 804–832); its read-only intrinsic projection deliberately excludes upstream identity, paired CMH, global BH, and publication (665–677). Step 08 and Step 09 validators and artifact-index projection call these at distinct boundaries. The Step 09 validator does not independently recompute CMH statistics; its separate Python/real-R oracle addresses that gap. Step 10 independently re-derives reference windows without rerunning R. The Snakefile's static processing-owner fence rejects even a schema-valid reassignment, as a direct workflow test demonstrates. These are independent protections, not equivalent duplicate callers. Step 08 numerical-oracle work remains open under `SCI-ORACLE-01`; no product saving is defensible here.

The earlier stage/evidence pairs also protect different points. Step 00a shell checks generated index membership before producer success, while its Python validator checks reference/contig/settings agreement. Step 00c shell checks generated FAI↔DICT sets; Python checks exact ordered FASTA↔sidecar agreement. Step 01 shell checks nonempty outputs; Python checks BAM/log/SJ structure. Step 06 producer checks samtools output and counts; its validator checks signatures and TSV arithmetic. Step 07 producer checks bcftools output before receipt, while its validator checks the published manifest/path/count projection without rerunning bcftools. For Steps 00a–07, these producer checks precede native publication; separate validators inspect visible outputs afterward. Step 02b/03 outputs are explicitly non-gating evidence. The Step 00a/00c/01/02b/03/06/07 source plus shared STAR/orientation/mpileup helpers changed only +88/−39 product lines in the provisional diff; even removing every changed line there cannot satisfy the gross target. This is source accounting, not permission to weaken any check.

### F15 — Gate and evidence ownership

The documentation structure checker validates tracked Markdown/Mermaid inventory, canonical headings, owner adjacency, and local links; its tests inject missing owners/pages and malformed links to verify the checker. The source-dependency gate instead checks Python import direction. Local validation composes lint, documentation, source edges, shell syntax, bytecode, manifests, and test lanes; hosted CI adds static, shell, guarded-R, coverage, and opt-in synthetic E2E with distinct evidence ceilings. No caller-complete gate deletion is established. `SHFMT_BIN` in `scripts/make_quality.mk` has no tracked invocation and is a one-line tooling candidate. A documentation gate change belongs to `ASSURANCE-01`; none of these candidates counts toward the product target.

### F16 — Private artifact-index facade

`_artifact_index/api.py` consists of re-exports and an `__all__` list. A tracked source/test/script/doc search found no static reference to this private facade, while independent contract goldens import its underlying `core`, `models`, and `records` modules. Its 30 product lines and one file are a concrete gross retirement candidate. Dynamic import, packaged API, and external-use checks remain before any deletion; net reduction has not been measured.

### F17 — Run-summary helper owner

`_run_summary/transaction.py` contains `_path_hash`, `_canonical_key`, and `_stable_unique` value helpers, with no transaction. Only same-owner `document.py` and `projection.py` import it. Inlining the one-use path projection and placing stable uniqueness beside its remaining caller could retire the 47-line file, but replacement lines must be counted. Deterministic summary JSON/TSV, schema, warning order, and independent goldens are protected; the owner README's module table would change. The estimated 10–25 net product lines are a hypothesis until a prototype diff exists.

### F18 — Report context state

`ReportContext` is a frozen dataclass, yet its `previous_snapshots` field retains a mutable mapping built during context admission. Publication uses only its truthiness to reject an existing transaction, then separately checks each stable disk path with `lexists`. A boolean could represent the retained decision without keeping mutable snapshot state. The complete-existing-output refusal test protects behavior. This is a state-shape candidate, not an observed publication bypass or a material product-line saving.

### F19 — Strict JSON admission owners

`contracts/orchestration/api.py` (89–137) and `contracts/artifacts/_artifact_contracts/schema.py` (23–53) both parse a JSON object while rejecting duplicate names and `NaN`/infinite numbers. Their closed schema-registry builders also look similar, but orchestration uses an explicit `$id` table and cached validators, while artifact contracts derive IDs and use different errors and order. Public exception types and diagnostic text differ. A narrow shared parser might reduce code if it preserves each owner's contract; a combined registry has no measured saving. Caller and invalid-input characterization must precede any move.

### F20 — Storage receipt admission

The storage owner builds deterministic receipt bytes, then admits retained direct, compute, and final receipts through `_json_object` (`qualification.py`, 330–337). That helper uses bare `json.loads`: an isolated standard-library probe made a duplicate `status` key resolve to the last value and accepted `NaN`. The storage test at `test_storage_inventory.py` 593–607 covers malformed JSON and a non-object, not these cases. Direct admission binds same-host identity including device ID (742–800); two-phase final admission binds retained compute/head receipts while intentionally allowing cross-node device IDs to differ (817–870). The raw receipt-byte digest is retained. This is an ambiguous/nonstandard input-admission finding, not a demonstrated qualification bypass. A repair needs direct and two-phase cases, owner error/compatibility review, and retained receipt protection; reusing contract parsing has no measured net saving.

### F21 — Stage BAM policy

The Step 02 producer's `validate_bam_pair` (lines 49–88) requires exactly one sample `ID`/`SM`/`LB` read group with `PL:ILLUMINA`, coordinate sorting, all alignment tags, and a nonempty BAI. Its separate input predicate (90–114) controls the no-rewrite hard-link path. Step 05's check (68–105) instead verifies the `ID`/`SM`, sort, tag, and BAI properties after GATK; it does not require `LB`/`PL`. Step 02 and Step 05 contracts and fake-samtools tests state these different guarantees. A shared parameterized check could add policy branches while weakening the prepublication/no-rewrite distinction. No whole-function removal or net saving is established.

### F22 — Shell parsing surface

Ten production shell owners use the existing shared `declare_required_arguments`, `assign_option_value`, and `require_arguments` helpers, yet keep 83 explicit `--option` dispatch arms. The repeated grammar is a possible bounded consolidation, but owner options include distinct defaults and optional fields, and direct command, diagnostic, help, and Make expansion fixtures protect the command surfaces. A generic parser is useful only if one measured prototype removes more total product code than it adds, preserves malformed/duplicate/missing-value behavior and option order where promised, and replaces all equivalent owner switches. No net reduction is booked from counting arms alone.

### F23 — Bounded TSV streaming

`step09.py` `_stream_projection_tsv` (582–611) and `scientific_context.py` `_stream_tsv` (292–324) both open UTF-8 with `newline=""`, use strict tab `csv.reader`, reject row-width drift, yield numbered dictionaries, and normalize I/O/Unicode/CSV failures. Step 09 admits a dynamic sample block; scientific context requires one fixed header. The existing `libraries/validation/tsv.py` `read_strict_tsv` materializes rows and has different error timing, so substituting it directly would regress bounded-memory behavior. A new narrow streaming helper in that existing owner might save 15–25 net lines, but exact diagnostics and both scientific caller paths need a measured prototype and direct tests.

### F24 — Scientific R unreachable checks

Step 10 R canonicalizes all nine output paths, then rejects duplicates and input collisions (`scientific_context_projection.R`, 938–950). No output argument changes before its later five-temporary-path duplicate check (991–1000), so the later subset check cannot fail after the earlier superset check. `make_receipt` also accepts a `catalog` parameter that its body never reads (846–903; one call at 1007). In Step 09 R, replicate IDs are nonempty, each condition set is duplicate-free, and control/treatment sets are equal before `replicate_order` is filtered by the control set (`step_09_cmh_validation.R`, 56–72); that filter and following length error are unreachable. These are source-level small candidates. An approved scientific change still needs exact invalid-input and output-path diagnostic tests; no R producer or scientific result was changed here.

### F25 — Report stylesheet selectors

The tracked CSS includes `.panel-grid`, `.status-panel`, `.status-value`, and grouped `.status-*` text-color selectors (`run_report.css`, 126–149 and 524–551). A search of source producers outside that stylesheet found no use of those classes. The one HTML template emits status as table text; its dynamic `figure-status-*`, `state-*-boundary`, and `legend-*` classes are live and outside this candidate. Removing the approximately 42 candidate CSS lines would alter newly rendered self-contained HTML and the stylesheet SHA-256 recorded in report context and receipt. Previously published reports retain their own bytes and provenance. The independent `report_html.sha256` golden uses synthetic inline CSS, so it does not cover this tracked stylesheet change. Treat retirement as an explicit output decision with exact rendering/provenance checks, not free deletion.

### F26 — Shared figure palette

`figures.py` (53–62) and `scientific_context_figures.py` (48–57) define byte-identical eight-color `_PAIR_COLORS` tuples and index them by pair order (870 and 712). The context module already imports helpers from `figures.py`; importing this palette could remove its duplicate literal for about nine net product lines. Both files are unchanged from the provisional base, so this would reduce stock/net growth, not gross additions. Deterministic SVG tests must confirm unchanged colors and hashes. Their repeated legend limit is not enough to share the full legend policy: the zero-pair branches differ.

### F27 — Figure asset normalization

`figure_models.py` (259–302) constructs the same `ScientificFigurePanel` for the single-asset case inside `validate()` and again inside `assets`. One validated asset projection might avoid about 15 lines, pending an exact diff. Current checks reject mixed/partial provenance and repeated panel IDs, and decoded assets have their own size/hash check distinct from producer SVG XML checks. Preserve these boundaries and verify existing single/panel rendering tests before any change. This unchanged baseline file cannot lower gross added lines at the provisional refs.

### F28 — Specialist manifest command

`emrys init manifests` is registered in `__main__.py` (174–185) and implemented by `onboarding.py` parser/handler (1524–1604). Guided Project Init also calls the shared `_draft_manifest_members` core, but the specialist route uniquely publishes standalone draft tables without creating a Project. The coordinator contract, cluster finding record, and direct tests retain that use. Removing only the route might save about 80–90 current product lines; deleting the shared drafting core would break guided Init. Because the route predates the provisional base, even an approved retirement saves zero gross integration additions. Operator need and public command compatibility remain undecided.

### F29 — High-growth coordinator decisions

The provisional product diff is concentrated in orchestration, but size did not establish redundant authority. Stop presentation (`control.py`, 2334–2592) uses `slurm_submission.py` (303–476) for exact request/controller/client admission, then separately re-admits Run/Task evidence after transport. Doctor repair (1657–1975) separates managed repair, shared-generation replacement, maintenance claims, Slurm/storage qualification, and retained failures. `materialization.py` (328–819 and 891–1281) builds distinct ordered Step commands and immutable Attempt provenance; its diff is only +69/−36. Moving those branches into generic tables or other packages has no measured net reduction and could change identities or recovery. A name-based screen found no unreferenced private top-level coordinator function; that is a screen, not dead-code proof.

### F30 — Generic manifest contract

The public `emrys validate manifest` route uses the 190-line `ingestion/sample_manifest_admission/validator.py`. Its `replicate` column is optional, and direct tests preserve that lighter schema. The configuration and Runbook guidance identify its five-column example as a generic-validator fixture, not a paired-CMH Project manifest. Project Init instead validates Step 08's exact manifest with required replicate and bound FASTQ identities (`step08.py`, 142–150 and 326–354; `onboarding.py`, 1125–1139). The command is registered in `__main__.py`, appears in operator documentation, and is invoked by two Make quality checks (`make_quality.mk`, 217–223). Deleting it would remove distinct accepted behavior and require caller-complete migration. It is an unchanged baseline owner: 190 gross current product lines, zero reduction of provisional gross additions.

### F31 — Workflow-profile derivation

`local_cmh_v2.json` has 12 owner tasks, 57 artifact templates, and 69 explicit `scope_selector` fields. Contract admission enforces the current fixed `scope_type`→selector map for both groups (`contracts/orchestration/api.py`, 330–342 and 387–400), making those fields derivable in this current profile. It also requires `semantic_owner_keys` and `required_owner_keys` to equal the task machine-key set (353–374), yet current materialization/task readers consume `required_owner_keys`, and `application_model.py` projects it into the immutable Execution Plan. Deleting fields from the JSON would change profile bytes/hash and retained identity; a replacement must migrate all writers, readers, schemas, tests, and old records. This is a separately approved profile/schema decision with no measured product-code saving.

### F32 — Runtime trust and process count

`runtime_availability/inspector.py` `runtime_file_bindings` (120–253) hashes the targets observed for a current Project runtime and compares sealed content; `run_coordinator/lifecycle.py` `_admit_required_tool_identity` (1041–1120) re-admits serialized Attempt identities at execution. These checks share path/hash mechanics but answer different trust-time questions. The installed runtime policy also has ten R namespace checks, each launched as a separate probe; the optimization campaign already records that process multiplicity as a measurement question. No duplicate authority or net code saving follows from merging the checks or processes by appearance.

The fixed probe roster does not establish recursive R dependency closure; `RUNTIME-CLOSURE-01` remains open. The FASTQ pair diagnostic is outside the workflow DAG and makes 21 logical reads per mate at the default 20-ID prefix; its owner already records byte/diagnostic constraints under `OPS-03`. Neither is a measured REDUCE product-code saving.

### F33 — Step 07 region evidence ceiling

The shared FAI/DICT parser intentionally permits empty contig names and zero lengths (`libraries/references/contigs.py`, 50–77, with direct tests), so the Step 07 producer's explicit FAI guard (`partitioned_cohort_mpileup/producer.py`, 184–185) is necessary before its native work. The Step 07 validator calls `libraries/validation/mpileup.py` `selector_ok` (130–153), which checks only nonempty rows and known first-column contigs for a regions file; the producer additionally handles gzip input and coordinate bounds. Existing validator tests characterize a compressed-region false failure and an out-of-bounds plain-BED false pass. These are known validation limits, not a reason to remove the producer guard or claim scientific completion. Repair needs separate evidence/contract review and cannot be booked as REDUCE saving.

## Coverage and remaining comparisons

This source pass has examined the major changed orchestration owners and representatives from every product family: reporting, contracts, analyses, stages, evidence, libraries, workflow, and ingestion, plus scripts, configuration, schemas, tests, and documentation. It has **not** completed a file-by-file caller and fault audit of all 204 product files. The 150 files unchanged by the provisional integration diff remain within REDUCE-01 scope; selected large unchanged owners were examined above, but the rest still need owner-by-owner comparison. The next comparison must prioritize high-growth coordinator branches, dynamic/package consumers of retirement candidates, exact fault behavior, and measurable replacement diffs. `SIZE-01`, `SCHEMA-01`, `ASSURANCE-01`, `DOCS-01`, and `OPS-03` retain their own decisions and acceptance.

## Selection boundary

Before a reduction slice, approve exact comparison refs and the 25% metric; select one bounded, caller-complete outcome; identify all preserved behavior and protections; and measure product code, tests, tooling, schemas, configuration, documentation, mutable state, and retained evidence separately. The current source pass does not establish a thousands-of-lines saving. Site and scientific evidence remain at their existing levels.
