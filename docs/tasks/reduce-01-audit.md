# REDUCE-01 repository audit

This is a repository-wide finding record for [REDUCE-01](backlog_matrix.md#maintainability-and-release). The backlog matrix remains the authority for status and acceptance. Finding IDs below are navigation aids, not another task-status list or permission to implement a proposal.

## Snapshot and measurement

The audited commit is `3a672fdf8e55b30efc63dea9aecc4a29d28a5f4d`. Its product source, tests, scripts, and configuration are unchanged from `42d02c5a`; the intervening commit changes documentation. The provisional comparison is the **local** merge base `f2c0149e73a685b6f3d5b162f3804edee7c78c10`. This is a pinned source snapshot; later branch movement requires a new comparison.

The snapshot contains 575 tracked files: `src/` 316, `tests/` 186, `docs/` 35, repository root 14, `configs/` 11, `scripts/` 6, `.github/` 3, `LICENSES/` 2, and `Projects/` 2. Under the repository's existing physical-line convention—tracked product `.py`, `.R`, `.sh`, `.css`, `.j2`, and `Snakefile`, excluding generated `renv/activate.R` and tooling `restore_r_environment.R`—203 product files contain 65,148 lines. The integration diff changes 54 of those files; this audit also examines older, unchanged owners.

| Surface in provisional diff | Added | Deleted | Accounting boundary |
| --- | ---: | ---: | --- |
| Product code | 11,885 | 3,169 | Only this surface can establish product-code reduction. |
| Tests and fixtures | 22,030 | 2,751 | Protection and evidence value require separate review. |
| Markdown and diagrams | 7,038 | 586 | Documentation deletion cannot offset product growth. |
| Schemas | 159 | 88 | Contract and compatibility decisions remain separate. |
| Repository scripts | 69 | 60 | Tooling is reported separately. |
| Configuration and governance | 510 | 257 | Includes product resource configuration. |
| Other tracked files | 1 | 0 | One `Projects/.gitkeep`; no product saving. |

For separate reduction scorecards under the same provisional refs and Markdown-before-tests classification, non-Markdown test/fixture stock grew from 134 files/61,524 physical lines to 137 files/80,803 lines (net +19,279). Markdown/diagram stock grew from 169 files/9,469 lines to 173 files/15,921 lines (net +6,452). Review any later test or documentation reduction against the integration snapshot and record the protection or useful context retained; neither stock changes the product target.

The categories above assign Markdown/diagrams before tests and sum to 41,692 additions and 6,911 deletions across 168 changed files. Orchestration alone accounts for 10,361 added and 2,692 deleted product lines in the provisional diff. The owner selected **net product-code growth** as the primary 25% measure, with test and documentation reductions counted separately. The exact comparison refs, counted product extensions, and move treatment remain open, so the target is **not measured as achieved**. If these provisional refs and product-code convention are selected, 25% means 2,179 fewer lines of net growth; the secondary gross-additions comparison would require 2,972 fewer added product lines. Retained evidence has not been proposed for deletion or counted as savings.

The product stock covers every `src/emrys` family under the stated convention:

| Family | Files | Physical lines |
| --- | ---: | ---: |
| Orchestration | 26 | 28,108 |
| Reporting | 47 | 12,274 |
| Contracts | 22 | 6,984 |
| Stages | 36 | 5,063 |
| Libraries | 33 | 4,574 |
| Analyses | 12 | 3,781 |
| Evidence | 20 | 3,165 |
| Workflow | 1 | 446 |
| Package root | 2 | 391 |
| Ingestion | 4 | 362 |
| **Total** | **203** | **65,148** |

The excluded resource-restoration script has 132 lines and remains in the repository audit as tooling. The 54 changed product files contain 37,993 current lines; 149 unchanged product files contain 27,155 lines (41.7% of current product code). Some large unchanged owners still need fine-grained review, including scientific-context projections and figures, Step 08/09 contracts, and storage qualification. Their size alone proves no reducible behavior. Coordinate size exceptions with `SIZE-01`; this audit must also inspect their callers and protections.

Evidence here is committed-source review, existing test/contract inspection, and the isolated probes identified below. Repository tests, hosted CI, installed interactive Watch, Slurm, Viking, scientific review, and biological interpretation were not performed for this audit.

## Findings matrix

| ID | Finding | Classification | Discovery and protected boundary | Reduction evidence and next check |
| --- | --- | --- | --- | --- |
| F01 | Measurement definition | Primary measure selected; refs/counts open | The [backlog row](backlog_matrix.md#maintainability-and-release) selects net product-code growth for the 25% target and separately counts test/documentation reductions. Direct PR #302 has no product-code diff, while whole-repository candidates include older code. | Review the fixed-ref proposal below before scorekeeping or implementation. Report gross product additions and each non-product surface separately. |
| F02 | Step 09/10 R validators | Consolidation candidate | [Step 09](../../src/emrys/analyses/paired_cmh_candidate_ranking/step_09_cmh_common.R) lines 108–122 and [Step 10](../../src/emrys/analyses/paired_cmh_candidate_ranking/scientific_context_projection/scientific_context_projection.R) lines 131–145 define identical safe-ID and hash validators. Both already load [`input_contract.R`](../../src/emrys/libraries/input_contract.R). Step 08's similar check lacks the same scalar guard. | Moving the two exact definitions to the existing shared R owner could save about 15 product lines, subject to an exact diff and diagnostic parity. Do not include Step 08 without equivalence proof. |
| F03 | Dashboard sample initialization | Consolidation candidate | [`dashboard.py`](../../src/emrys/orchestration/run_coordinator/dashboard.py) lines 1097–1114 register samples on the wildcard event. Lines 1137–1143 and 1167–1175 repeat initialization. Current parser dataflow gives those later branches no path to a new sample. | The source reachability argument needs direct parser tests for active, completed, missing, repeated, and reordered log events before removing either branch; potential saving is about 16 product lines. |
| F04 | Dashboard stage placeholder | Representation cleanup | All 16 `STAGES` rows in [`dashboard.py`](../../src/emrys/orchestration/run_coordinator/dashboard.py) lines 26–204 contain an unused third `None`; tuple consumers and one test assume six fields. | Removing it could delete about 16 physical lines but offers little behavioral simplification. It must not stand in for a meaningful 25% reduction. |
| F05 | Inspection and Watch overlap | Preserve; architecture undecided | [`control.py`](../../src/emrys/orchestration/run_coordinator/control.py) calls installed Watch for raw jobs, submissions, and Runs. [Inspection presentation](../../src/emrys/orchestration/run_coordinator/_inspection_presentation.py) owns terminal interaction and fresh action handoff; the 2,442-line [dashboard](../../src/emrys/orchestration/run_coordinator/dashboard.py) still supplies diagnostic parsing/rendering and raw selection; [inspection](../../src/emrys/orchestration/run_coordinator/inspection.py) admits Run completion and recovery. | The standalone dashboard wrapper is already retired. A static call graph reaches all 52 remaining top-level dashboard functions/classes from live entry roots; no standalone-only deletion or unified-model saving is established. Map exact caller/fault parity before a measured prototype. |
| F06 | Interactive log regex | Potential responsiveness defect | [Inspection presentation](../../src/emrys/orchestration/run_coordinator/_inspection_presentation.py) lines 1021–1044 applies an arbitrary user regex during interactive painting and search submission. The tail has byte and line limits, but regex runtime has no bound. The [Runbook](../operations/RUNBOOK.md) lines 96–103 promises regex search. | An isolated standard-library pattern against a 30-character line exceeded a 0.5-second subprocess limit. Rich was unavailable, so the Watch path was not reproduced. A separate repair must preserve regex behavior and navigation; book no REDUCE saving. |
| F07 | Step 10 shell worker | Preserve pending proof | The 192-line [Step 10 worker](../../src/emrys/analyses/paired_cmh_candidate_ranking/scientific_context_projection/scientific_context_projection.sh) checks input hashes and staged receipt payloads before successful exit. The runner owns publication, but Step 10's grouped Python validation occurs **after** native publication ([`task.py`](../../src/emrys/orchestration/run_coordinator/task.py), lines 1742–1758 and 2732–2782). | Retiring the shell would remove a distinct prepublication check unless an equal or stronger check is placed at that boundary. Planner, package roster, direct command, tests, and owner documentation would also need caller-complete migration. No safe saving is measured. |
| F08 | Reporting transaction publishers | Preserve; prototype undecided | Artifact-index and HTML publishers already share file, signal, and exclusive-publication helpers. Their directory modes, source rechecks, payload validation, receipt ordering, rollback anchors, and interrupt behavior differ ([artifact-index publisher](../../src/emrys/reporting/_artifact_index/publication.py); [HTML publisher](../../src/emrys/reporting/_run_report/publication.py)). | The proposed common engine has no demonstrated net reduction. Preserve independent fault coverage; count savings only after one prototype replaces both loops without adding a parallel policy layer. |
| F09 | Replace-in-place publishers | Defective recovery boundaries; separate repair | [Validation publication](../../src/emrys/libraries/validation/publication.py) and [reference-provenance reconciliation](../../src/emrys/evidence/reference_provenance/reconciler.py) share a broad stage/backup/restore shape, but publish one versus three outputs. Their owner guidance records ownership or restoration gaps. | A shared primitive requires fault and ownership proof after recovery behavior is settled. These are not safe deletion estimates; preserve backups, partials, locks, and recovery evidence. |
| F10 | Resource benchmark recommendation | Function-level defect reproduced; tooling | [Benchmark script](../../scripts/benchmark_stage_resources.py) lines 322–365 discards failed trials before recommending a value. An isolated call with one passing and one failing repetition emitted `recommended=yes`; `run` returns nonzero for any failed trial by source control flow. The current summary test lacks that mixed case. [`SETUP-02`](backlog_matrix.md#deferred-operational-work) requires rejecting any failed repetition while the helper remains. | Add a mixed-repetition full-command characterization before a separately selected repair. Later coordinated retirement is already owned by SETUP-02. Neither action supplies product-code savings; retain raw trial evidence. |
| F11 | Root `.env.example` | Stale configuration candidate | [`.env.example`](../../.env.example) instructs copying seven variables with no other tracked reader. The current [loader](../../src/emrys/orchestration/run_coordinator/onboarding.py) lines 106–109 and 153–190 accepts a versioned `.env` with Projects root, site, and optional log root; it ignores a copied example lacking the version marker. | Confirm external operator use, then decide whether to retire the stale file or replace it with a current example. Eleven configuration lines; zero product-code saving. Keep `.env` ignored. |
| F12 | Viking resource YAML duplication | Undecided; preserve | The resource block in [the Viking profile](../../configs/execution_profile.csu_viking_ev_pum1.yaml) lines 5–43 matches the packaged [default](../../src/emrys/orchestration/run_coordinator/resources/default_execution.yaml). Explicit fields bind source provenance and affect resume inheritance ([`execution_profile.py`](../../src/emrys/orchestration/run_coordinator/execution_profile.py), lines 416–444). | The 39 repeated configuration lines are not a safe mechanical deletion. Compare new-Run and resume identity, hashes, and accepted site policy before a decision; zero product saving is established. |
| F13 | Narrow coordinator mechanics | Candidate; low yield | Explicit terminal confirmation and token-bound Slurm schema lists repeat small mechanics across coordinator owners. Request-record publication and recorded scheduler streams already have single or shared owners; directory synchronization wrappers enforce different trust and recovery checks. | Audit each complete caller set and fault behavior before sharing anything. Current source estimates are single-digit product lines, not evidence of a 25% path. |
| F14 | Scientific and evidence lookalikes | Preserve | Step 08/09 parsers accept different schemas and scientific policies. Step 09 computation and independent validation, Step 10 reference-window re-derivation, and the Snakefile's static owner fence detect distinct failures. | Do not count independent oracles, cross-checks, or different admission boundaries as duplication. Changes require their own scientific/contract review and equal-strength protection. |
| F15 | Documentation, tests, CI, and formatting | Separate owner review | Documentation structure checks, their failure-injection tests, CI preflight, synthetic E2E, scientific oracles, and retained measurements cover different failures or evidence levels. `SHFMT_BIN` in [`make_quality.mk`](../../scripts/make_quality.mk) has no invocation. | Review gate value under `ASSURANCE-01`, prose under `DOCS-01`, and surviving scripts under `OPS-03`. Removing the unused Make setting would save one tooling line; none of these changes counts as product reduction. |
| F16 | Artifact-index API facade | Retirement candidate | The 30-line private [`_artifact_index/api.py`](../../src/emrys/reporting/_artifact_index/api.py) re-exports sibling helpers and an artifact-contract API alias. No tracked static caller was found at this commit; independent contract goldens import the underlying modules. | Confirm dynamic imports, packaged API expectations, and external use before deleting. It is 30 current product lines/one file, but an unchanged baseline file, so retiring it saves zero provisional gross additions; net effect needs an exact diff. |
| F17 | Run-summary `transaction.py` | Owner consolidation candidate | The 47-line [`_run_summary/transaction.py`](../../src/emrys/reporting/_run_summary/transaction.py) contains deterministic value helpers, no transaction. Only same-owner document/projection modules import it. | Inline or move helpers within that owner, then check summary JSON/TSV, ordering, schema, and independent goldens. Estimated net saving 10–25 product lines and one file, subject to a prototype. |
| F18 | Report previous-output snapshots | State simplification candidate | Frozen `ReportContext` retains a mutable `previous_snapshots` mapping, but publication uses only its truthiness; context admission first validates the existing receipt and output hashes. | A boolean may avoid retained mutable state only if the full receipt/roster/hash preflight, its diagnostics, and publication refusal survive. No material line saving is established. |
| F19 | Strict JSON admission | Consolidation candidate; undecided | [Orchestration contracts](../../src/emrys/contracts/orchestration/api.py) and [artifact contracts](../../src/emrys/contracts/artifacts/_artifact_contracts/schema.py) each reject duplicate keys and nonstandard numbers. Their schema registries have different ID, caching, error, and diagnostic policies. | Compare complete callers and diagnostics before sharing the narrow parse policy; do not merge registries by appearance. Net product saving is unmeasured. |
| F20 | Storage receipt JSON | Potential admission defect; separate review | [Storage qualification](../../src/emrys/evidence/storage_inventory/qualification.py) uses bare `json.loads` for direct and two-phase receipts. Duplicate keys are last-wins and `NaN` is accepted in an isolated parser probe; current tests cover malformed syntax/non-objects only. | Characterize duplicate/nonstandard values through actual receipt admission, preserve raw-byte hashes and direct versus cross-node identity policy, then choose a repair. No bypass or REDUCE saving is established. |
| F21 | Stage BAM checks | Preserve distinct policy | [Canonical BAM](../../src/emrys/stages/canonical_bam/step_02_sort_index_bam.sh) and [Split N Cigar](../../src/emrys/stages/split_n_cigar/step_05_split_n_cigar_reads.sh) both check a BAM/index pair, but the former requires `LB`/`PL` and decides whether input bytes can be reused; the latter checks a narrower post-GATK contract. | Do not unify their checks without matching decision inputs, diagnostics, timing, and tests. No net saving is established. |
| F22 | Shell worker argument switches | Consolidation candidate; low yield | Ten shell owners share [`argument_parsing.sh`](../../src/emrys/libraries/argument_parsing.sh) but repeat 83 option-dispatch arms. Required/optional options, defaults, diagnostics, and published help differ. | Prototype a caller-complete roster parser only if it reduces total code while preserving each CLI and shell fault check. Gross repeated arms are not a net saving. |
| F23 | Strict TSV streaming readers | Conditional consolidation candidate | [Step 09](../../src/emrys/contracts/scientific_evidence/step09.py) and [scientific context](../../src/emrys/contracts/scientific_evidence/scientific_context.py) repeat a UTF-8/strict-CSV row stream without whole-file materialization. Their dynamic versus fixed header policies differ; the existing shared strict TSV reader materializes rows. | A shared helper might save 15–25 net product lines. A contract-to-library import needs an approved topology exception or a contracts-local owner; count that cost and preserve diagnostics and streaming behavior. |
| F24 | Scientific R dead guards | Small dead-code candidates | [Step 10 R](../../src/emrys/analyses/paired_cmh_candidate_ranking/scientific_context_projection/scientific_context_projection.R) repeats a five-output collision check after checking the full nine-output set, and passes an unused `catalog` parameter; Step 09 filters an already equal replicate set. | Verify diagnostics and edge cases, then remove only the unreachable checks in an approved scientific slice. About 10 lines for the Step 10 guard; other savings are smaller. |
| F25 | Unproduced report CSS | Output-change candidate | About 42 lines of [`run_report.css`](../../src/emrys/reporting/styles/run_report.css) style `.panel-grid`, `.status-panel`, `.status-value`, and `.status-*` classes with no producer in audited source. | Confirm all renderer/plugin paths. Deletion changes newly published report bytes and recorded CSS hash, so it needs an explicit output/provenance decision; no evidence is deleted. |
| F26 | Paired-figure palette | Consolidation candidate | [Scientific figures](../../src/emrys/reporting/paired_cmh_candidate_ranking_report/figures.py) and [context figures](../../src/emrys/reporting/paired_cmh_candidate_ranking_report/scientific_context_figures.py) use the same eight-color tuple by pair index. | Import one palette from the existing owner and verify deterministic SVG hashes; about 9 net product lines are possible. Other plotting and legend behavior differs. |
| F27 | Figure asset construction | State/owner simplification candidate | [`figure_models.py`](../../src/emrys/reporting/paired_cmh_candidate_ranking_report/figure_models.py) constructs the same single-asset panel in validation and `assets`. | A single validated projection may reduce about 15 lines, pending a diff and duplicate-ID, mixed-provenance, and hash/size parity tests. |
| F28 | Specialist manifest drafting route | Public-surface decision; preserve pending review | [`emrys init manifests`](../../src/emrys/orchestration/run_coordinator/onboarding.py) publishes standalone drafts using a core also used by guided Project Init. The public route is documented and tested; most, but not all, route lines predate the provisional integration. | Retirement could remove roughly 90 current product lines only after an explicit operator/contract decision. That would lower net growth; gross-addition impact needs an exact route-only diff. |
| F29 | Large coordinator owners | Preserve distinct boundaries | Eight coordinator files account for 7,767 of 11,885 provisional gross product additions (65.35%). Stop, Watch, Doctor repair, runtime reuse, scheduler observation, native-child closure, and [task materialization](../../src/emrys/orchestration/run_coordinator/materialization.py) serve different decisions. | No caller-complete large deletion was found. Continue tracing high-growth branches and fault tests; line count or a moved table is not a saving. |
| F30 | Generic manifest validator | Public-surface decision; preserve pending review | The 190-line [manifest validator](../../src/emrys/ingestion/sample_manifest_admission/validator.py) supports optional `replicate`, unlike Project Init's Step 08 contract. It has a public CLI, two Make checks, owner tests, and operator documentation. | Retire only after a caller-complete compatibility decision. This unchanged baseline module could lower net product growth and one file count, but saves zero provisional gross additions. Count its dedicated tests separately. |
| F31 | Derivable workflow-profile fields | Schema/config decision | The [profile](../../src/emrys/workflow/contracts/local_cmh_v2.json) repeats 69 selectors derivable from scope type and two 12-key owner lists validated against tasks. Readers use these fields in materialization, workflow, and immutable plan identity. | Coordinate with `PROFILE-CONTRACT-01`/`SCHEMA-01`; no mechanical deletion or product saving. Preserve profile hashes, retained Runs, identity, and recovery. |
| F32 | Runtime binding rechecks | Preserve distinct trust times | [Runtime inspection](../../src/emrys/evidence/runtime_availability/inspector.py) hashes current observed targets and seals, while [lifecycle admission](../../src/emrys/orchestration/run_coordinator/lifecycle.py) rechecks serialized Attempt identities at execution. | Similar path/hash checks serve different authority moments. No caller-complete reduction is established; separate process-count optimization from code compression. |
| F33 | Step 07 region-validation limits | Known evidence gap; preserve producer guard | [Shared contig parsing](../../src/emrys/libraries/references/contigs.py) is intentionally permissive, so [Step 07's producer](../../src/emrys/stages/partitioned_cohort_mpileup/producer.py) rejects invalid FAI contigs itself. The [validator](../../src/emrys/stages/partitioned_cohort_mpileup/validator.py) has tested region-file false-pass/false-fail limits. | Do not delete the producer guard as duplication. Repairing validator limits needs separate scientific/evidence review; no REDUCE saving is established. |
| F34 | Artifact-inventory TSV writer | Conditional consolidation candidate | [Orchestration projection](../../src/emrys/contracts/orchestration/projection.py) lines 66–77 repeats the deterministic TSV writer already in [validation TSV](../../src/emrys/libraries/validation/tsv.py) lines 18–42. An isolated source-function probe produced equal bytes and errors for selected rows. | A contract-to-library import is prohibited by the current [source topology](../../src/emrys/contracts/SOURCE_TOPOLOGY.md#dependency-direction). Approve that dependency seam and prove exact bundle-byte/hash and fault parity before replacement. About 8–11 current product lines may be removable, but the unchanged baseline owner contributes zero provisional gross additions. |
| F35 | Declared-path dot components | Function-level admission defects reproduced | [Artifact path validation](../../src/emrys/contracts/artifacts/_artifact_contracts/identity.py) and [reference inventory](../../src/emrys/evidence/reference_provenance/_reference_inventory.py) check `Path.parts` for `.` after that API has discarded it. Isolated actual-function probes admitted `results/./one.tsv` and `./fasta`, respectively. The [artifact JSON path schema](../../src/emrys/contracts/schemas/artifacts/v1/common.schema.json) rejects dot components. | Characterize both owner routes and their distinct path/receipt policies before repair; do not infer a path escape or share policy by appearance. No full reporting/Run reproduction or REDUCE saving is established; both implementations are unchanged baseline code. |
| F36 | Duplicate report HTML attributes | Function-level admission ambiguity reproduced | Both [core report validation](../../src/emrys/reporting/_run_report/validation.py) and [paired-CMH validation](../../src/emrys/reporting/paired_cmh_candidate_ranking_report/validation.py) collapse raw HTML attributes with `dict(attrs)`. A duplicated `data-run-id` retained only the last value in an isolated core-inspector probe. | Characterize duplicate attributes in a full staged-report fixture before a separate repair. Do not infer an exploit or a published incorrect report; neither unchanged validator yields provisional gross-addition savings. |
| F37 | Step 00b normalized BED names | Producer/validator mismatch reproduced | [GTF conversion](../../src/emrys/stages/gtf_to_bed12/converter.py) maps distinct `tx`/gene `g` and `tx|g` transcript inputs to the same `tx|g` BED name. An isolated validator report marks `unique_transcript_names=fail`; the Project-admission GTF precheck covers contigs and bounds, not name uniqueness. | Decide whether to reject the collision earlier or improve the producer diagnostic; preserve the validator check. No full Run outcome or REDUCE saving is established. The owner predates the provisional diff. |
| F38 | Generic manifest empty condition | Contract intent undecided; function behavior reproduced | The public [generic manifest validator](../../src/emrys/ingestion/sample_manifest_admission/validator.py) accepts a sample with an empty `condition` and returns an empty condition set in an isolated call. Its owner README requires the column but does not explicitly require a nonempty value; the distinct Step 08 contract does. | Decide the generic command's content policy and characterize its CLI before repair. Do not silently replace its lighter contract with Step 08's. The unchanged owner offers no provisional gross-addition saving. |
| F39 | Picard metrics duplicate header | Function-level admission ambiguity reproduced | [Shared Picard parsing](../../src/emrys/libraries/quality/picard.py) checks required column membership, then `dict(zip(...))` retains the last value of a duplicate `READ_PAIRS_EXAMINED` column. An isolated conflicting-row probe returned a passing result; [Step 04 validation](../../src/emrys/stages/duplicate_marking/validator.py) consumes it. | Add a direct duplicate-header case and decide parser policy before a separate repair. No actual Picard output, full validator result, or REDUCE saving is established; this is unchanged baseline code. |
| F40 | Slurm capacity source label | Function-level provenance defect reproduced | [Allocation observation](../../src/emrys/orchestration/run_coordinator/capacity.py) accepts `SLURM_CPUS_ON_NODE` as fallback but still labels a per-CPU memory product as `SLURM_CPUS_PER_TASK`. An isolated probe found correct numeric capacity with the wrong source label; [resource policy](../../src/emrys/orchestration/run_coordinator/resource_policy.py) records that label in the immutable Attempt policy. | Characterize the crossed environment case and repair only future records without rewriting retained Attempts. The label change is not a REDUCE line saving; no live Slurm run was tested. |
| F41 | Incomplete raw scheduler roster | Function-level selection defect reproduced | [`scheduler_candidates`](../../src/emrys/orchestration/run_coordinator/dashboard.py) limits the roster to 50 before [`resolve_named_selection`](../../src/emrys/orchestration/run_coordinator/dashboard.py) checks name uniqueness. An isolated 51-job probe admitted one apparent match for final job selection although two names matched. The raw [command adapter](../../src/emrys/orchestration/run_coordinator/scheduler_observation.py) also maps a failed query to empty text, allowing an accounting-only partial roster. | Characterize truncation and query failure at the public raw selector, then require complete or explicitly ambiguous discovery. Preserve exact job identity checks; no live Slurm or REDUCE saving is established. |
| F42 | Raw scheduler capture size | Source-level responsiveness risk | The [command adapter](../../src/emrys/orchestration/run_coordinator/scheduler_observation.py) captures entire `squeue`/`sacct` stdout; [`scheduler_candidates`](../../src/emrys/orchestration/run_coordinator/dashboard.py) parses it before returning at most 50 candidates. | Bound capture and parsing only in a separately reviewed repair that preserves failure/ambiguity semantics. No large-output or live Slurm reproduction and no REDUCE saving are established. |
| F43 | Submission request version prose | Owner documentation drift | The [coordinator contract](../../src/emrys/orchestration/run_coordinator/CONTRACT.md) lines 487–490 describes v2/v3 selected-request binding while [submission source](../../src/emrys/orchestration/run_coordinator/slurm_submission.py) admits v4 and [inspection source](../../src/emrys/orchestration/run_coordinator/_submission_inspection.py) uses it. | Reconcile exact v4 behavior, compatibility, and tests in the owner contract. This is documentation work, not product-code reduction. |
| F44 | Hand-maintained Bash syntax roster | Separate tooling reduction candidate | [`make_quality.mk`](../../scripts/make_quality.mk) lists 13 Bash files for `bash -n`, while Git tracks 28 `.sh` files. `lint` already ShellChecks the tracked roster, so this does not establish an overall CI coverage gap. | Derive the syntax roster from tracked shells while preserving the test override and checking Make expansion and gate behavior. This removes a second list and covers all tracked shells in the syntax-only check, with zero product-code saving. |
| F45 | Temporary cluster campaign prose | Conditional documentation reduction candidate | The [cluster backlog](cluster_verification_backlog.md) and [campaign charter](cluster_verification_campaign.md) contain 4,107 and 210 lines. The backlog still owns 61 CV card dispositions and acceptance detail. | Once every delegated card has a terminal disposition or transfer, reconcile references and transfer lasting decisions/evidence under the [closure checklist](backlog_matrix.md#cluster-verification-closure-checklist). Measure retired documentation separately. No current deletion is justified. |
| F46 | Watch tail freshness and retained text | Function-level presentation defect reproduced | [`read_tail`](../../src/emrys/orchestration/run_coordinator/_inspection_presentation.py) rebuilds a `StreamCache` from prior file state without prior text. A pending read therefore shows no prior text while claiming retained history; pending and unavailable reads receive a current “Tail observed” date without bytes observed. | Characterize same-source pending, unavailable, replacement, and rotation in full Watch paths before a separate repair. Preserve bounded waits and stale-data honesty. No REDUCE saving is established. |
| F47 | Rebuilt unavailable selected track | Small reporting reduction candidate | [`scientific_context_figures.py`](../../src/emrys/reporting/paired_cmh_candidate_ranking_report/scientific_context_figures.py) repeats the selected-track unavailable specification. The builder discards the third item of the public unavailable roster, then rebuilds it with the selected-candidate reason. | One private helper could save about 10 net product lines, pending an exact diff and figure-ID, reason, roster, and SVG parity checks. |
| F48 | Mixed-case SVG URL syntax | Function-level validation gap reproduced | [`figures._validated_svg`](../../src/emrys/reporting/paired_cmh_candidate_ranking_report/figures.py) scans style text with case-insensitive `url(...)` but scans attribute values case-sensitively. An isolated actual-function probe admitted external `URL(...)`/`uRl(...)` attribute values. | Characterize generated Matplotlib SVG and report embedding before a separate repair. Later base64/hash and HTML checks do not inspect embedded SVG URL syntax; no published report or browser behavior was demonstrated. No material REDUCE saving. |
| F49 | Malformed compute-root shape | Public-route diagnostic defect reproduced | [`qualification._validate_compute`](../../src/emrys/evidence/storage_inventory/qualification.py) calls `.get("path")` on `row["root"]` without checking its type. A tiny retained compute receipt with `root: null` made the public finalize route raise raw `AttributeError` instead of returning its controlled error exit. | Add malformed-root fault coverage and repair receipt admission under the storage owner, preserving retained evidence and the two-phase policy. No REDUCE saving. |
| F50 | Step 10 receipt binding after first read | Core-function interleaving gap; outer defenses preserved | [`scientific_context.py`](../../src/emrys/contracts/scientific_evidence/scientific_context.py) hashes receipt-bound paths before later semantic reads, without a final hash comparison inside the transaction function. A deterministic test hook changed one summary's bytes after the first hash; core admission returned with a stale receipt hash. | Trace all outer snapshot/publication rechecks before any repair: the CLI and artifact-index paths have later defenses. No full Run bypass, published stale report, or REDUCE saving is established. |
| F51 | Step 08 raw count trailing comma | Function-level lexical gap reproduced | [`_step_08_vcf_counts.R`](../../src/emrys/stages/cohort_candidate_preprocessing/_step_08_vcf_counts.R) splits count vectors with `strsplit`, which drops a final empty field. The actual raw-count check accepted `1,`; a tiny VCF with trailing-comma `INFO/AD` and `FORMAT/AD` passed the raw VCF count pass. | Characterize downstream VariantAnnotation and complete Step 08 behavior before a separate scientific repair. The owner contract requires exact count widths; no full Run or REDUCE saving is established. |
| F52 | Repeated test-scope boilerplate | Separate documentation reduction candidate | Eight owner test READMEs repeat an identical five-line paragraph about worker scratch, shared runner checks, grouped validation, and evidence limits. The [test-tree guide](../../tests/README.md) already owns shared evidence limits. | Move the common test-scope explanation to that guide and retain a short owner link in each README, then verify all links and local exceptions. Estimated 20–27 net documentation lines; zero product/test reduction. |
| F53 | Step 02 read-group substring admission | Worker-level contract defect reproduced | [Canonical BAM worker](../../src/emrys/stages/canonical_bam/step_02_sort_index_bam.sh) uses substring tests for `LB` and `PL` in both input reuse and staged-output checks. A tiny fake-samtools worker run hard-linked an input with `LB:sampleExtra`/`PL:ILLUMINAX` as canonical output. | Add exact-field wrong-suffix cases and repair the worker admission; the [separate validator](../../src/emrys/stages/canonical_bam/validator.py) currently promises only exact `ID`/`SM`. No real BAM, full Run, or REDUCE saving was established. |
| F54 | Table-caption check with void elements | Parser-function validation gap reproduced | [`ReportHTMLInspector`](../../src/emrys/reporting/_run_report/validation.py) increments table depth for a void `<br>` but receives no closing tag for it. An isolated class probe let a captionless `emrys-table` with `<br>` finish without a table error. | Characterize a full staged report and preserve heading, resource, and table checks in any owner repair. No published incorrect report or REDUCE saving is established. |
| F55 | Analysis output descriptor shape | Function-level admission gap reproduced | [`analyses._safe_artifact`](../../src/emrys/analyses/__init__.py) accepts a string or a tuple of nonstrings as `expected_header`, and an exact zero-row declaration with header-only output forbidden. Later [artifact TSV admission](../../src/emrys/reporting/_artifact_index/_text_tabular.py) cannot fulfill those declarations. | Add malformed installed-provider descriptor cases and reject contradictions at the first admission boundary. Built-in declarations are valid; no Project/Run outcome or REDUCE saving is established. |
| F56 | Step 06 rounded fraction boundary | Producer/validator mismatch reproduced | [Step 06 producer](../../src/emrys/stages/mechanical_orientation/producer.py) writes `assigned_fraction` to six decimals; [its validator](../../src/emrys/stages/mechanical_orientation/validator.py) compares binary floats to `5e-7`. A tiny actual-function fixture at 3/128 generated a failing arithmetic check. | Compare decimal or integer-scaled rounding under a separate owner repair, preserving all count checks. No real BAM/Run proof or REDUCE saving is established. |

## Source and protection details

### F01 — Reproducible measurement

The provisional stock uses `git ls-tree -r --name-only 3a672fdf` and physical lines of the tracked product extensions under `src/`, excluding generated `renv/activate.R` and tooling `restore_r_environment.R` as the [prior campaign](backlog_matrix.md#repository-maintenance) did. The provisional diff uses `git diff --numstat --no-renames f2c0149e 3a672fdf`; classification gives Markdown/diagrams precedence over tests. The direct [PR #302](https://github.com/lab-cats/EMRYS/pull/302) diff (`93b5c45a`→`42d02c5a`) changes 12 files and no product code; its branch supplies the audit starting state, not a useful denominator for a product-code target. The provisional `f2c0149e` comparison includes the ancestor integration stack. Product stock grew from 200 files/56,432 lines to 203 files/65,148 lines. Three new product files are `_inspection_presentation.py`, `_submission_inspection.py`, and `scheduler_observation.py`; no product file was retired.

**Owner decision:** net product-code growth is the primary 25% measure. Tests and documentation should also lose redundant or excessive material where protection and useful context survive, with those reductions counted separately. **Still proposed for review:** fix `f2c0149e` as the pre-integration baseline and compare it with the final, approved reduction head under the stated tracked product-code convention. Report gross added product lines alongside net growth. This keeps whole-repository candidates such as F16/F17 and F23–F27 visible without booking tests, documentation, configuration, or evidence deletion as product savings. Coordinate separate test/documentation reductions with `ASSURANCE-01`/`DOCS-01`; preserve the workflow's no-product-file-growth and meaningful-net-reduction guardrails independently.

For a reproducible final comparison, freeze the full baseline (`f2c0149e73a685b6f3d5b162f3804edee7c78c10`), integration snapshot (`3a672fdf8e55b30efc63dea9aecc4a29d28a5f4d`), and a final head descending from that snapshot. Count physical lines in the selected tracked Git blobs at each ref. Under the existing product convention, baseline and integration contain 56,432 and 65,148 lines, so their 8,716-line net growth would need to fall to at most 6,537; a final product stock of at most 62,969 would satisfy the numeric threshold. Report the final-head change since integration, the secondary gross diff, product-file count, and separately measured test, documentation, script, schema, configuration, mutable-state, and evidence changes. Within-product moves leave the primary stock unchanged; cross-surface moves need role review so relocating implementation outside `src/` is not booked as product deletion. Attribute unrelated product changes separately. These are proposed accounting rules until the refs and treatment are approved; line arithmetic alone does not establish caller-complete compression or behavioral parity.

| Interpretation against provisional `f2c0149e`→`3a672fdf` | Starting value | 25% target for final head | Effect of deleting unchanged baseline code |
| --- | ---: | ---: | --- |
| Gross added product lines | 11,885 | At most 8,913 (2,972 fewer) | No reduction to gross additions. |
| Net product-code growth **(selected primary)** | 8,716 | At most 6,537 (2,179 less growth) | Reduces net growth. |

These are conditional arithmetic thresholds, not achieved savings. The exact integration refs, final head, counted extensions, and move/rename treatment still need an accepted decision before implementation or scorekeeping. Publish product, tests/protections, scripts, schemas, configuration, documentation, mutable state, and retained evidence separately; no test or documentation reduction offsets product-code growth.

### F02 — Shared R validators

Step 09 and Step 10 each define the same `validate_safe_id` and `validate_hash` bodies. Their entrypoints load the existing `libraries/input_contract.R` before these definitions. Moving the two definitions there would remove about 30 duplicated lines and add about 15 shared lines, for a provisional net saving of 15 product lines. Step 09 validates Step 08 receipt hashes; Step 10 checks six supplied hashes. Step 08's similar safe-ID check lacks the scalar-length guard and is outside this candidate. A caller-complete change needs exact invalid scalar, character, and hash diagnostic parity plus the real-R producer suites. No such change or test has run.

These owners also have near-identical `write_tsv` bodies (`step_09_cmh_common.R`, 160–170; `scientific_context_projection.R`, 789–799). They differ in their postwrite diagnostic label. Step 08's writer does not have the same parent and postwrite checks, so it is outside this adjacency. Preserving the two diagnostics in one helper may save only a few net lines; no replacement diff or R fault test exists.

### F03 — Parser reachability

A job starts with an empty wildcard string. The wildcard event is the only assignment of a nonempty wildcard string and registers its sample immediately. Completion and final active-job processing read that same value; `samples` is never cleared. This makes the two later initialization blocks unreachable under the current parser by source inspection. Before removal, assert sample order and history for malformed and repeated events, and retain the contract's distinction between log observations and verified Run progress.

### F04 — Stage-table placeholder

Every `STAGES` row has a third `None`, while tuple consumers and a shape test expect the six-field representation. Removing the placeholder would also require updating that expectation. This is a representation cleanup with no demonstrated reduction in decisions, branches, or owned behavior; its 16 physical lines must remain separate from meaningful product compression.

### F05 — Installed Watch and inspection

At the audited commit, `control.watch_from_args` (lines 3009–3075) fixes a Project Run, associated request, or raw scheduler selection and reuses `inspect_from_args` (2632–2801). All modes enter the single terminal owner in `_inspection_presentation.watch` (1069–1368). Its `WatchSnapshot` (396–417) keeps request, application, Run, scheduler, and trace observations separately dated. Explicit refresh re-admits the selected request/application/Run; timer refresh reads only scheduler and diagnostic streams (538–705, 1172–1194). Raw scheduler mode cannot acquire Project/Run action authority; Run-only mode reports scheduler identity unknown. Static Inspect and Watch both use `project_run`; Watch also uses the live dashboard parser, stream cache, and renderer. Action callbacks carry exact selectors into newly parsed CLI handlers after terminal restoration. Direct tests cover fixed selection, read-free painting, stale-evidence clearing, raw-mode separation, view parity, and action handoff. `DASHBOARD-RETIRE-01` already retired the standalone wrapper/callers and retains shared dashboard code pending its own CI/institutional verification. The remaining 2,442-line module changed +914/−1,123 (net −209) in the provisional comparison and has no separate installed entry point; static reachability from Watch and raw-selection roots covers all 52 top-level functions/classes. That screen does not prove every branch necessary, but identifies no standalone-only function island. If its entire stock were removed, a replacement of at most 263 lines would be needed to meet the provisional 2,179-line target from this module alone, before accounting for other changes. No such caller-complete replacement exists. Any redesign must show parity for static, Project Run, associated-request, raw/offline, interactive, and nonterminal modes before a measured saving is claimed.

### F06 — Regex cost

A query-length cap alone would not solve catastrophic backtracking: the standard-library pattern `(a+)+$` against 29 `a` characters followed by `!` exceeded a 0.5-second isolated subprocess limit. This does not reproduce installed Watch because Rich was unavailable. The audit has not chosen a dependency or changed the documented regex feature. Exact interactive behavior remains unverified.

### F07 — Step 10 check order

The shell does not own locking, final links, or rollback; the runner does. Its `validate_receipt_payloads` checks hashes and row counts for all four staged outputs before producer success (`scientific_context_projection.sh`, lines 80–139). The runner's prepublication validation set contains only Steps 08 and 09 (`task.py`, 1742–1758); Step 10's grouped validator runs after native publication (2732–2782). The coordinator contract distinguishes producer abort before publication from failed validation after commit (lines 978–988 and 1117–1131). The shell test checks a valid fake receipt and one broken receipt, not each payload mismatch. Retiring the shell requires prepublication parity for all payloads, input hashes, receipt shape, planner/direct callers, and recovery/abort fault paths. No safe 192-line saving is established.

### F08 — Reporting transactions

The coordinator has one production dispatch for each reporting publisher. The artifact-index publisher descriptor-walks a destination below its admitted artifact root, rechecks native sources and package, stages TSV/QC/JSON, then links the JSON manifest last. The HTML publisher can create independent output directories, validates staged and final scientific/evidence HTML, and links a TSV receipt last. Their rollback, lock, and anchor handling differ. The existing `_files.py`, `_signals.py`, and `exclusive_publication.py` already share equivalent low-level mechanics. Direct reporting tests exercise mid-publish failures and retained recovery state. No measured common engine is shorter or equal in protection; independent tests and receipt ordering remain necessary.

### F09 — Replace-in-place recovery

The validation library's one-file publisher serves four validator owners through `validation/runtime.py` (lines 27–49). It validates the staged and predecessor reports, then replaces the final (`validation/publication.py`, 13–70). Existing characterization tests show a late foreign final can be deleted during rollback and a failed restore can leave the predecessor backup while releasing the lock. Reference-provenance reconciliation publishes three outputs and moves all predecessors before entering its rollback handler (`reconciler.py`, 83–126). Its failed-restore test leaves backups with the lock removed; the owner README also records partial backup-move and diagnostic-loss risks. `exclusive_publication.publish_exclusive` covers absent or exactly expected single-file bytes, not this complete replacement policy. Repair and consolidation need separate ownership/fault review. The 58-line validation publisher is gross exposed surface, not a booked net saving, and none of its failure tests are deletion offsets.

### F10 — Mixed benchmark trials

`_write_summary` groups passing rows only, computes a median for those rows, and can recommend the value (script lines 322–366). An isolated call to that exact function with fabricated pass/fail rows for one value emitted `successful_repetitions=1` and `recommended=yes`. The local Python lacked PyYAML, so the otherwise unused import was stubbed for this function-only probe; no benchmark commands or repository tests ran. By source control flow, `run` retains each failed row and returns 1 if any trial failed (477–510). The combined command exit and summary need a full-command fixture before claiming an end-to-end reproduction. Existing tests cover all-pass and all-fail values, not mixed repetitions. A focused case should assert raw-row retention, nonzero exit, and no recommendation for that value. This is a separate tooling repair, with no product-code saving.

The separate `SETUP-02` retirement is already approved only after the optimization campaign no longer needs the helper. Its current surface is 535 script lines and a 368-line dedicated test file, plus CLI-roster and documentation references. A caller-complete retirement then could remove those script and test surfaces, with actual net reductions measured separately and no credit against product-code growth. It cannot be booked while the helper remains a campaign dependency, and raw trials, scientific-equivalence fixtures, and retained evidence must survive.

### F11 — Copied configuration

The example's instruction leads to a `.env` lacking `EMRYS_ENV_VERSION=1`. The current loader skips an unmarked file (`onboarding.py`, 153–170), so the example's promised settings do not take effect through EMRYS's saved-setting path. `emrys setup` then refuses to create new settings if that file exists (215–220). The seven old variable names and `.env.example` path have no other tracked readers or references in the audited source. This is a source-derived operator trap, not a reproduced operator session; external scripts remain unverified consumers. Retirement or replacement belongs to configuration and onboarding review, with zero product-code saving.

### F12 — Resource values and provenance

The Viking profile's resource values match the packaged defaults at the audited commit, and a test checks declaration equality. The provisional base had a different fixed Viking resource policy, so this equality is an integration change. The current test does not compare source provenance. `load_execution_profile` gives explicit resource fields the selected source path and SHA-256 and sets `computational_resources_explicit=True` (416–444). A placement-only profile instead inherits the predecessor's resource policy on resume (`control.py`, 695–704 and 843–858). The raw profile source hash also enters binding. Removing the repeated YAML block could change new-Run and resume identity even if the values remain equal. The 39-line configuration candidate needs a paired identity/provenance/resume comparison; it cannot count as product savings.

### F13 — Small coordinator repeats

Three `y`/`yes` confirmation helpers in `control.py`, `doctor.py`, and `onboarding.py` repeat a small decision, although prompt and diagnostic ownership may differ. `_submission_inspection.py` hardcodes the same token-bound v2–v4 version set represented by `slurm_submission._REQUEST_SCHEMAS[1:]`; the trust and compatibility boundary needs comparison before sharing it. `request.json` has one writer, and scheduler submit/stop already share transcript publication. Directory-sync helpers vary in no-follow, identity, type, and failure semantics; sharing their surface shape could lose protection. These candidates offer only single-digit product-line savings unless a measured prototype proves otherwise.

### F14 — Scientific and evidence independence

Step 08 admission validates site rows, scope counts, sample DP/AD/AF, and carried location (`contracts/scientific_evidence/step08.py`, 483–555). Step 09 additionally binds analysis identity and exact carry-forward of Step 08 metadata/sample fields (`step09.py`, 804–832); its read-only intrinsic projection deliberately excludes upstream identity, paired CMH, global BH, and publication (665–677). Step 08 and Step 09 validators and artifact-index projection call these at distinct boundaries. The Step 09 validator does not independently recompute CMH statistics; its separate Python/real-R oracle addresses that gap. Step 10 independently re-derives reference windows without rerunning R. The Snakefile's static processing-owner fence rejects even a schema-valid reassignment, as a direct workflow test demonstrates. These are independent protections, not equivalent duplicate callers. Step 08 numerical-oracle work remains open under `SCI-ORACLE-01`; no product saving is defensible here.

The earlier stage/evidence pairs also protect different points. Step 00a shell checks generated index membership before producer success, while its Python validator checks reference/contig/settings agreement. Step 00c shell checks generated FAI↔DICT sets; Python checks exact ordered FASTA↔sidecar agreement. Step 01 shell checks nonempty outputs; Python checks BAM/log/SJ structure. Step 06 producer checks samtools output and counts; its validator checks signatures and TSV arithmetic. Step 07 producer checks bcftools output before receipt, while its validator checks the published manifest/path/count projection without rerunning bcftools. For Steps 00a–07, these producer checks precede native publication; separate validators inspect visible outputs afterward. Step 02b/03 outputs are explicitly non-gating evidence. Step 02b's shell producer preserves a nonempty, zero-exit samtools quickcheck stream as success, while its validator recognizes the synthetic empty-success marker; the owner contract and tests record that difference, so it is a preserved mismatch rather than a deletion candidate. `all_pass.py` checks the generic report's shape, identities, and statuses; each owner builds its expected check roster before that later gate, so these are not duplicate authorities. The Step 00a/00c/01/02b/03/06/07 source plus shared STAR/orientation/mpileup helpers changed only +88/−39 product lines in the provisional diff; even removing every changed line there cannot satisfy the gross target. This is source accounting, not permission to weaken any check.

### F15 — Gate and evidence ownership

The documentation structure checker validates tracked Markdown/Mermaid inventory, canonical headings, owner adjacency, and local links; its tests inject missing owners/pages and malformed links to verify the checker. The source-dependency gate instead checks Python import direction. Local validation composes lint, documentation, source edges, shell syntax, bytecode, manifests, and test lanes; hosted CI adds static, shell, guarded-R, coverage, and opt-in synthetic E2E with distinct evidence ceilings. Eight coordinator test files contribute 16,033 of the provisional 22,030 test/fixture additions (72.8%); the largest is `test_materialization.py` at +4,170/−789. An AST-body screen of 1,535 top-level Python test functions found no exact duplicate bodies; this is not proof that all cases are semantically unique. An exact top-level helper-body comparison across the eight high-growth files found only one duplicated two-line snapshot helper (`test_materialization.py`, 169–170; `test_slurm_submission.py`, 56–57); sharing it would add fixture/import machinery without a demonstrated net saving. Apparent overlap between `test_inspection_presentation.py` (589–667) and `test_dashboard.py` (559–718) exercises the same stream scenarios at the Watch wrapper and cache primitive boundaries, so deleting either set would need a surviving oracle at both boundaries. The v2/v3 request fixtures remain live compatibility checks because `slurm_submission.py` still admits those versions. Distinct identity, fault, and recovery cases need case-by-case review before any test compression. No caller-complete gate or test deletion is established. `SHFMT_BIN` in `scripts/make_quality.mk` has no tracked invocation and is a one-line tooling candidate. A documentation gate change belongs to `ASSURANCE-01`; none of these candidates counts toward the product target.

### F16 — Private artifact-index facade

`_artifact_index/api.py` re-exports sibling helpers, an artifact-contract API alias, and an `__all__` list. A tracked source/test/script/doc/CI search found no static or string-dynamic reference to this private facade; independent contract goldens import its underlying `core`, `models`, and `records` modules. `pyproject.toml` discovers the broad `emrys*` package tree but declares no entry point for this facade, and the wheel smoke test checks public onboarding modules rather than this private path. Its 30 current product lines and one file are a concrete retirement candidate, but deleting an unchanged baseline file saves zero gross additions under the provisional comparison. Installed-wheel inventory and external-import expectations remain unverified; net reduction has not been measured.

### F17 — Run-summary helper owner

`_run_summary/transaction.py` contains `_path_hash`, `_canonical_key`, and `_stable_unique` value helpers, with no transaction. Only same-owner `document.py` and `projection.py` import it. Inlining the one-use path projection and placing stable uniqueness beside its remaining caller could retire the 47-line file, but replacement lines must be counted. Deterministic summary JSON/TSV, schema, warning order, and independent goldens are protected; the owner README's module table would change. The estimated 10–25 net product lines are a hypothesis until a prototype diff exists.

### F18 — Report context state

`ReportContext` is a frozen dataclass, yet its `previous_snapshots` field retains a mutable mapping built during context admission. `_existing_outputs` first validates any retained receipt, member roster, hashes, and sizes (`_run_report/context.py`, 306–338). Publication then uses the mapping's truthiness to reject an existing transaction and separately checks each stable disk path with `lexists` (`_run_report/publication.py`, 102–109). A boolean could represent the retained decision without keeping mutable snapshot state only if that full preflight and its diagnostics remain. The complete-existing-output refusal test protects behavior. This is a state-shape candidate, not an observed publication bypass or a material product-line saving.

### F19 — Strict JSON admission owners

`contracts/orchestration/api.py` (89–137) and `contracts/artifacts/_artifact_contracts/schema.py` (23–53) both parse a JSON object while rejecting duplicate names and `NaN`/infinite numbers. Their closed schema-registry builders also look similar, but orchestration uses an explicit `$id` table and cached validators, while artifact contracts derive IDs and use different errors and order. Public exception types and diagnostic text differ. A narrow shared parser might reduce code if it preserves each owner's contract; a combined registry has no measured saving. Caller and invalid-input characterization must precede any move.

### F20 — Storage receipt admission

The storage owner builds deterministic receipt bytes, then admits retained direct, compute, and final receipts through `_json_object` (`qualification.py`, 330–337). That helper uses bare `json.loads`: an isolated standard-library probe parsed `{"status":"failed","status":"qualified"}` as `qualified` and accepted `{"x":NaN}`. The storage test at `test_storage_inventory.py` 593–607 covers malformed JSON and a non-object, not these cases. Direct admission binds same-host identity including device ID (742–800); two-phase final admission binds retained compute/head receipts while intentionally allowing cross-node device IDs to differ (817–870). The raw receipt-byte digest is retained. This is an ambiguous/nonstandard input-admission finding, not a demonstrated qualification bypass. A repair needs direct and two-phase cases, owner error/compatibility review, and retained receipt protection; reusing contract parsing has no measured net saving.

### F21 — Stage BAM policy

The Step 02 producer's `validate_bam_pair` (lines 49–88) requires exactly one sample `ID`/`SM`/`LB` read group with `PL:ILLUMINA`, coordinate sorting, all alignment tags, and a nonempty BAI. Its separate input predicate (90–114) controls the no-rewrite hard-link path. Step 05's check (68–105) instead verifies the `ID`/`SM`, sort, tag, and BAI properties after GATK; it does not require `LB`/`PL`. Step 02 and Step 05 contracts and fake-samtools tests state these different guarantees. A shared parameterized check could add policy branches while weakening the prepublication/no-rewrite distinction. No whole-function removal or net saving is established.

### F22 — Shell parsing surface

Ten production shell owners use the existing shared `declare_required_arguments`, `assign_option_value`, and `require_arguments` helpers, yet keep 83 explicit `--option` dispatch arms. The repeated grammar is a possible bounded consolidation, but owner options include distinct defaults and optional fields, and direct command, diagnostic, help, and Make expansion fixtures protect the command surfaces. A generic parser is useful only if one measured prototype removes more total product code than it adds, preserves malformed/duplicate/missing-value behavior and option order where promised, and replaces all equivalent owner switches. No net reduction is booked from counting arms alone.

### F23 — Streaming TSV readers

`step09.py` `_stream_projection_tsv` (582–611) and `scientific_context.py` `_stream_tsv` (292–324) both open UTF-8 with `newline=""`, use strict tab `csv.reader`, reject row-width drift, yield numbered dictionaries, and normalize I/O/Unicode/CSV failures. Step 09 admits a dynamic sample block; scientific context requires one fixed header. Neither loop establishes a per-row byte cap; the useful property is streaming without whole-file materialization. The existing `libraries/validation/tsv.py` `read_strict_tsv` materializes rows and has different error timing, so substituting it directly would regress that property. A narrow helper in that existing library would require a separately approved contract-to-library topology exception; a contracts-local owner is another possibility. The estimated 15–25 net lines must include any gate/documentation change. Exact diagnostics and both scientific caller paths need a measured prototype and direct tests.

### F24 — Scientific R unreachable checks

Step 10 R canonicalizes all nine output paths, then rejects duplicates and input collisions (`scientific_context_projection.R`, 938–950). No output argument changes before its later five-temporary-path duplicate check (991–1000), so the later subset check cannot fail after the earlier superset check. `make_receipt` also accepts a `catalog` parameter that its body never reads (846–903; one call at 1007). In Step 09 R, replicate IDs are nonempty, each condition set is duplicate-free, and control/treatment sets are equal before `replicate_order` is filtered by the control set (`step_09_cmh_validation.R`, 56–72); that filter and following length error are unreachable. These are source-level small candidates. An approved scientific change still needs exact invalid-input and output-path diagnostic tests; no R producer or scientific result was changed here.

### F25 — Report stylesheet selectors

The tracked CSS includes `.panel-grid`, `.status-panel`, `.status-value`, and grouped `.status-*` text-color selectors (`run_report.css`, 126–149 and 524–551). A search of source producers outside that stylesheet found no use of those classes. The one HTML template emits status as table text; its dynamic `figure-status-*`, `state-*-boundary`, and `legend-*` classes are live and outside this candidate. Removing the approximately 42 candidate CSS lines would alter newly rendered self-contained HTML and the stylesheet SHA-256 recorded in report context and receipt. Previously published reports retain their own bytes and provenance. The independent `report_html.sha256` golden uses synthetic inline CSS, so it does not cover this tracked stylesheet change. Treat retirement as an explicit output decision with exact rendering/provenance checks, not free deletion.

### F26 — Shared figure palette

`figures.py` (53–62) and `scientific_context_figures.py` (48–57) define byte-identical eight-color `_PAIR_COLORS` tuples and index them by pair order (870 and 712). The context module already imports helpers from `figures.py`; importing this palette could remove its duplicate literal for about nine net product lines. Both files are unchanged from the provisional base, so this would reduce stock/net growth, not gross additions. Deterministic SVG tests must confirm unchanged colors and hashes. Their repeated legend limit is not enough to share the full legend policy: the zero-pair branches differ.

### F27 — Figure asset normalization

`figure_models.py` (259–302) constructs the same `ScientificFigurePanel` for the single-asset case inside `validate()` and again inside `assets`. One validated asset projection might avoid about 15 lines, pending an exact diff. Current checks reject mixed/partial provenance and repeated panel IDs, and decoded assets have their own size/hash check distinct from producer SVG XML checks. Preserve these boundaries and verify existing single/panel rendering tests before any change. This unchanged baseline file cannot lower gross added lines at the provisional refs.

### F28 — Specialist manifest command

`emrys init manifests` is registered in `__main__.py` (174–185) and implemented by `onboarding.py` parser/handler (1524–1604). Guided Project Init also calls the shared `_draft_manifest_members` core, but the specialist route uniquely publishes standalone draft tables without creating a Project. The installed-wheel help test asserts the route (`test_package_distribution.py`, 385–409); direct tests cover its behavior and failure paths (`test_onboarding.py`, 1416–1472 and 1659–1823). The coordinator contract and cluster finding record retain advanced structural drafting. No tracked Make or workflow invocation was found, but external invocations remain unknown. Removing only the 80-line route plus roughly ten dispatcher/normalization lines might save about 90 current product lines; deleting the shared drafting core would break guided Init. Roughly 300 route-focused onboarding-test lines, help, reserved-name routing, and owner prose would need review and separate accounting. Most route logic predates the provisional base, but integration changed `--region`, `--verbose`, and presentation lines within it, so a gross-addition saving cannot be called zero without an exact route-only diff. The selected net metric would fall by any verified net product deletion. Operator need and public command compatibility remain undecided.

### F29 — High-growth coordinator decisions

The provisional product diff is concentrated in orchestration: `control.py`, `onboarding.py`, `doctor.py`, `slurm_submission.py`, `task.py`, and the three new inspection/scheduler files contribute 7,767 of 11,885 gross product additions (65.35%). Across all orchestration files the provisional diff is +10,361/−2,692, or 7,669 of the repository's 8,716 net product-code growth (88.0%). The 33 changed product files outside orchestration contribute +1,524/−477, net +1,047. Under the selected net measure, safe reductions anywhere in the repo may count; no source family is mathematically required. For the secondary gross view, even deleting every non-orchestration addition would leave 1,448 lines of the conditional 2,972-line target to find in orchestration. This is arithmetic prioritization, not a safe deletion estimate. No caller-complete path near the primary 2,179-line threshold was found. Stop presentation (`control.py`, 2334–2592) uses `slurm_submission.py` (303–476) for exact request/controller/client admission, then separately re-admits Run/Task evidence after transport. `inspect_from_args` separates raw job, request, and Run selectors; Watch maps its shorthand into that fixed path. `_submission_inspection.py` bounds stable reads and separates request-token from Run-history scans. `scheduler_observation.py` owns exact job identity/resource/timing used by several coordinator surfaces. Onboarding's create-absent tree publisher is already shared by Project Init, manifest draft, and synthetic fixture; its donor-seal reuse path makes a different decision from ordinary runtime discovery. Doctor timing and repair, and Task's Linux descendant closure, have direct fault/contract protection. `materialization.py` (328–819 and 891–1281) builds distinct ordered Step commands and immutable Attempt provenance; its diff is only +69/−36. Moving these branches into generic tables or other packages has no measured net reduction and could change identities or recovery. A name-based screen found no unreferenced private top-level coordinator function; that is a screen, not dead-code proof.

A deeper caller/fault trace covered 3,696 additions in `control.py`, `onboarding.py`, and `doctor.py`: resume resource checks occur at separate submission and compute admissions; Project Init preview avoids FASTQ hashing while creation makes one admission pass; runtime reuse needs a donor seal and maintenance/replacement authority absent from ordinary discovery; Doctor's head, compute, managed, and shared repair paths have distinct claims and fault tests. The `slurm_submission.py`, `task.py`, `_submission_inspection.py`, and `scheduler_observation.py` trace likewise found active callers for closed request records, filtered Stop, frozen submission plans, stable inspection reads, native-child containment, terminal evidence, and exact scheduler identity. Similar path and directory checks occur at different mutation times. These observations support preservation boundaries, not a positive net-line reduction or proof that no future reduction exists.

### F30 — Generic manifest contract

The public `emrys validate manifest` route uses the 190-line `ingestion/sample_manifest_admission/validator.py`. Its `replicate` column is optional, and direct tests preserve that lighter schema. The configuration and Runbook guidance identify its five-column example as a generic-validator fixture, not a paired-CMH Project manifest. Project Init instead validates Step 08's exact manifest with required replicate and bound FASTQ identities (`step08.py`, 142–150 and 326–354; `onboarding.py`, 1125–1139). The command is registered in `__main__.py` and the exact source-topology CLI roster, appears in operator documentation and public-help tests, and is invoked by two Make quality checks (`make_quality.mk`, 217–223) plus hosted minimum-version smoke. Deleting it would require a public compatibility decision and migration of Make, CI, help tests, docs, and topology before any caller-complete retirement. Its unchanged 190-line module plus CLI wiring offers about 192 raw current product lines and one file, before replacement. The dedicated 264-line test file, subprocess coverage policy, generic fixture, and owner/operator docs require separate accounting; the FASTQ shell diagnostic is a distinct route to keep. Unlike F28, the module itself contributes zero gross additions in the provisional diff, but a verified retirement would lower net growth. Retiring both public routes would offer roughly 280–290 raw current product lines, only about 13% of the provisional 2,179-line reduction target before replacement, with little shared implementation to consolidate.

### F31 — Workflow-profile derivation

`local_cmh_v2.json` has 12 owner tasks, 57 artifact templates, and 69 explicit `scope_selector` fields. Contract admission enforces the current fixed `scope_type`→selector map for both groups (`contracts/orchestration/api.py`, 330–342 and 387–400), making those fields derivable in this current profile. It also requires `semantic_owner_keys` and `required_owner_keys` to equal the task machine-key set (353–374), yet current materialization/task readers consume `required_owner_keys`, and `application_model.py` projects it into the immutable Execution Plan. Deleting fields from the JSON would change profile bytes/hash and retained identity; a replacement must migrate all writers, readers, schemas, tests, and old records. This is a separately approved profile/schema decision with no measured product-code saving.

### F32 — Runtime trust and process count

`runtime_availability/inspector.py` `runtime_file_bindings` (120–253) hashes the targets observed for a current Project runtime and compares sealed content; `run_coordinator/lifecycle.py` `_admit_required_tool_identity` (1041–1120) re-admits serialized Attempt identities at execution. These checks share path/hash mechanics but answer different trust-time questions. The installed runtime policy also has ten R namespace checks, each launched as a separate probe; the optimization campaign already records that process multiplicity as a measurement question. No duplicate authority or net code saving follows from merging the checks or processes by appearance.

The fixed probe roster does not establish recursive R dependency closure; `RUNTIME-CLOSURE-01` remains open. The FASTQ pair diagnostic is outside the workflow DAG and makes 21 logical reads per mate at the default 20-ID prefix; its owner already records byte/diagnostic constraints under `OPS-03`. Neither is a measured REDUCE product-code saving.

### F33 — Step 07 region evidence ceiling

The shared FAI/DICT parser intentionally permits empty contig names and zero lengths (`libraries/references/contigs.py`, 50–77, with direct tests), so the Step 07 producer's explicit FAI guard (`partitioned_cohort_mpileup/producer.py`, 184–185) is necessary before its native work. The Step 07 validator calls `libraries/validation/mpileup.py` `selector_ok` (130–153), which checks only nonempty rows and known first-column contigs for a regions file; the producer additionally handles gzip input and coordinate bounds. Existing validator tests characterize a compressed-region false failure and an out-of-bounds plain-BED false pass. These are known validation limits, not a reason to remove the producer guard or claim scientific completion. Repair needs separate evidence/contract review and cannot be booked as REDUCE saving.

### F34 — Inventory TSV writer dependency

`contracts/orchestration/projection.py` builds artifact-inventory bytes with `csv.DictWriter`, the fixed `INVENTORY_HEADER`, tab separators, LF line endings, strict extra-field rejection, and UTF-8 encoding. Its only in-module caller is `build_reporting_bundle` (129), whose bytes flow to `run_coordinator/reporting_boundary.py` (463–474). `libraries/validation/tsv.py` already provides the same writer through `tsv_bytes`. In a dependency-free probe extracting the actual source functions, empty, ordinary, quoted Unicode/newline/tab, and missing-field cases returned equal bytes; an extra-field case raised the same exception. The fixed header is a tuple, while the shared helper converts it to a list; those selected outputs agree. This is function-level evidence, not package-level or bundle/hash parity.

The current source topology permits `contracts/` to import other contracts and only named implementation exceptions. It has no projection-to-validation-TSV exception, and the source-dependency gate enforces that roster. Reusing the writer would need a separately approved dependency seam, an exact replacement diff, and projection/integration tests that preserve inventory bytes, hashes, and errors. Retiring the local function and imports could save roughly 8–11 current product lines. Because this code predates the provisional comparison, that would lower stock/net growth but not gross additions.

### F35 — Dot components in inventory paths

`validate_resolved_path` (`_artifact_contracts/identity.py`, 43–66) intends to reject `.` and `..` path components, but `Path(value).parts` normalizes away `.` before the check. An isolated call to the actual `validate_inventory` function admitted a one-row standalone TSV inventory with `source_path=results/./one.tsv`; only an unavailable schema-module import was stubbed for that probe. `inventory.py` (139–147) calls the helper on each TSV source path without JSON Schema admission, while the artifact JSON common schema (19–23) rejects the same dot component. Existing contract tests cover `../`, `//`, and template syntax across JSON and inventory routes, but not `/./`.

The separate reference-provenance inventory uses the same ineffective `Path.parts` check (`_reference_inventory.py`, 65–70). An isolated actual `reconciler.load_inventory` call with its eight required roles admitted `./fasta`, resolved it to `fasta`, and retained the declared string; rendering writes that string to its artifacts TSV. Its tests cover malformed, symlink, and escape inputs, not dot components. Neither probe establishes a physical-path escape or a full reporting/Run outcome. Repairs need owner-specific path and retained-provenance characterization; similar syntax does not prove equivalent sharing policy. There is no measured code reduction, and both implementations predate the provisional comparison.

### F36 — Report attribute admission

The core and paired-CMH HTML inspectors both convert the parser's attribute list to a dictionary (`_run_report/validation.py`, 209–215; `paired_cmh_candidate_ranking_report/validation.py`, 41–47), discarding duplicate names. The core later compares the collapsed `<main>` attributes to expected provenance (436–472). An isolated probe extracting the actual core inspector parsed `<main data-run-id="forged" data-run-id="expected">` as `data-run-id=expected`; no duplicate flag survived. Staged and published report bytes pass through the core validation boundary, and current active-markup tests do not cover this case. A full report fixture must establish the exact admitted/published outcome before any repair claim. This is an input ambiguity, not a demonstrated exploit or published incorrect report. Both validators are unchanged baseline code, so no provisional gross-addition saving follows.

### F37 — Step 00b name collision

`gtf_to_bed12/converter.py` `_build_bed_name` (104–112) joins a cleaned transcript ID and optional gene ID with `|`. Two converter-accepted exon inputs with `transcript_id "tx"; gene_id "g";` and `transcript_id "tx|g";` therefore produced two BED12 rows named `tx|g` in an isolated actual-converter probe. The actual report builder marked `gtf_transcript_agreement=pass` but `unique_transcript_names=fail`; the shared BED inspection and Step 00b validator require unique names. Project admission's GTF precheck covers contigs and bounds but does not detect this normalized-name collision (`onboarding.py`, 1717–1736). The validator records a failed check row, but its shared report runtime returns zero even for fail rows (`libraries/validation/runtime.py`, 26–45). This probe does not establish an incorrect accepted scientific output or a full Run outcome. A separate owner review should choose earlier refusal or a clearer conversion diagnostic without weakening the validator check. No REDUCE saving is established, and the converter predates the provisional comparison.

### F38 — Generic manifest condition policy

`ingestion/sample_manifest_admission/validator.py` (112–160) requires nonempty sample IDs and FASTQ paths but adds `condition` to the summary only when it is nonempty. An isolated call to the actual `validate_manifest` with one otherwise valid row ending in an empty condition returned `ManifestSummary(sample_count=1, conditions=frozenset(), ...)`. The owner README lists `condition` among required columns and says the command rejects empty identities; it does not say explicitly whether a present condition may be empty. Step 08's paired-CMH admission rejects empty required values, but this generic public CLI has a lighter contract and is used by quality checks. The next step is a contract decision and a full CLI characterization, not a presumed Step 08 rule or a REDUCE saving. This file is unchanged in the provisional comparison.

### F39 — Picard column ambiguity

`libraries/quality/picard.py` (19–29) checks that required header names are present and the one row has the same width, then converts the header/row pair with `dict(zip(...))`. An isolated call to actual `parse_duplication_metrics` accepted a header containing `READ_PAIRS_EXAMINED` twice with conflicting values `1` and `10`, returning `(True, ...)` with `pairs=10`. The Step 04 validator calls this parser (`duplicate_marking/validator.py`, 68–70); its contract requires exactly one metrics row, and direct helper tests cover normal, reordered, and malformed cases but not a duplicate header. This does not establish that Picard emits such a header or that an end-to-end report is accepted. A parser policy decision and direct invalid-input characterization precede any repair. This unchanged helper offers no provisional gross-addition saving.

### F40 — Allocation source provenance

`run_coordinator/capacity.py` (155–177) falls back from `SLURM_CPUS_PER_TASK` to `SLURM_CPUS_ON_NODE` but always labels `SLURM_MEM_PER_CPU` multiplication with the former name. An isolated extraction of the actual observation functions with process-visible 16 cores/16,384 MiB and `SLURM_JOB_ID=42`, `SLURM_CPUS_ON_NODE=8`, `SLURM_MEM_PER_CPU=1024` returned the correct 8 cores/8,192 MiB and a source string falsely naming `SLURM_CPUS_PER_TASK`. `resource_policy.py` (264–284) persists that string in the immutable effective policy record. Current tests cover each variable combination separately but not this crossing. This is new-record provenance labeling, not an observed capacity error or live Slurm proof. A repair must preserve retained Attempt records and add the crossed case; it offers no measured REDUCE saving.

### F41 — Candidate-roster completeness

`dashboard.scheduler_candidates` (566–660) parses live `squeue` and recent `sacct` rows, sorts them, then returns only the first 50. `resolve_named_selection` (782–804) checks uniqueness after that truncation. An isolated probe extracting those actual functions with 51 mocked current-user jobs placed one exact name at IDs 1000 and 950, with the latter outside the first 50. The resolver passed 1000 as the apparent unique match to a stubbed final `scheduler_candidate_selection` instead of reporting ambiguity; no full public selection was exercised. Existing name-selection tests cover one and two matches within the visible roster, not a hidden 51st match. The retained cluster finding says duplicate names require a numeric ID.

`scheduler_observation.command_bytes` (54–68) returns `None` for failed or timed-out scheduler queries, while `command_text` (71–73) maps that result to the same empty string as successful empty output. A separate source-function probe with failed `squeue` and one accounting row returned a sole candidate, although the live roster was unknown. Exact `observe_job` has its own fail-closed query policy; it is not evidence that raw discovery is complete. The public raw selector and no-ID path need direct fault fixtures before a repair. Neither probe used live Slurm or establishes an incorrect Run association; no REDUCE saving follows.

### F42 — Capture before candidate limit

`scheduler_observation.command_bytes` uses `subprocess.run(stdout=PIPE)` to capture all scheduler output before it checks return status. `command_text` decodes the whole result, and `dashboard.scheduler_candidates` materializes live and recent dictionaries before slicing to 50. Per-job metadata functions also use the unbounded adapter. The coordinator contract already discloses that exact selected-request replies are capped after capture, so that separate path has no strict subprocess-memory guarantee either. The 50-result limit does not bound raw discovery capture or parse cost. This is a source-level responsiveness/resource risk, not a measured failure or a deletion candidate. Any repair must keep ambiguous and failed scheduler observations distinct and avoid treating a smaller partial roster as complete.

### F43 — v4 request contract wording

The coordinator contract's selected-submission paragraph (487–490) describes v2/v3 request binding and v3 name proof, although the same contract documents v4 elsewhere (412 and 438). The actual request roster includes `emrys.submission-request.v4` (`slurm_submission.py`, 55–59), and `_submission_inspection.py` (423–430) admits that schema. This localized prose drift needs a source/test comparison of v4 fields and its distinct acceptance behavior before an owner correction; merely replacing the version number could hide a policy change. It is not evidence of a product reduction.

### F44 — Bash syntax roster

`scripts/make_quality.mk` (25–38) maintains 13 explicit Bash paths for its syntax-only `bash -n` gate (208). Git tracks 28 `.sh` files at the audited commit. `validation-static` and `smoke` use the explicit roster; `lint` independently runs ShellCheck over all tracked `.sh` files, so the difference is a second inventory and a narrower syntax-only check, not proof that all other shell files escape CI. An isolated `bash -n` pass over all 28 tracked files succeeded. Deriving the syntax roster from the same Git inventory could remove about 13 hand-maintained Make lines while preserving a path override for focused tests. The Make expansion fixtures, syntax-gate tests, and behavior when Git inventory is unavailable need review in a selected tooling slice. This is not product-code saving.

### F45 — Conditional cluster-document retirement

The cluster verification backlog is 4,107 lines and its campaign charter 210 lines at the audited commit. The backlog has 61 CV card headings (`CV-01`–`CV-27`, `CV-U01`–`CV-U33`, and `CV-UX-01`) and remains the delegated acceptance authority for many `Verification pending` or open cards. The main matrix's closure checklist (145–152) requires a terminal disposition or named transfer for each card, live-reference reconciliation, and transfer of E01–E12, exact hosted/artifact records, lasting decisions, and evidence before retiring the backlog and charter together. Those conditions are not met in this source audit. The 4,317 lines describe a substantial *conditional* documentation reduction, subject to measuring actual transfers and retirement later under `DOCS-01`; they are not removable now or credit against net product-code growth. Evidence deletion retains separate authority.

### F46 — Watch tail observation and history

`_inspection_presentation.read_tail` (371–393) creates a fresh `dashboard.StreamCache` with only `previous.state` for the same source. `StreamCache` initializes an empty data buffer (`dashboard.py`, 352–365), then a pending read reports “retained history is from the previous observation” (408–411). In an isolated probe using the actual extracted source functions and a stubbed reader, a previous same-source tail containing `old` produced a pending `StreamTail` with empty text and a retained state; the rendered plain snapshot omitted `old` but printed the retention diagnostic. A failed read correctly withheld old text, but both pending and failed results gained the current fallback date at `read_tail` lines 389–391 and rendered as “Tail observed” (874) despite observing no bytes. The probe did not import the installed package or run full Watch, and used no live file or Slurm. The next owner characterization should cover same-source pending, unavailable, replacement, and rotation, and decide whether a previous verified observation should remain visible with an explicitly stale date. Any repair must preserve bounded waiting, source identity, and refusal to present stale bytes as fresh. No code reduction is established.

### F47 — Selected-track unavailable construction

`scientific_context_figures.py` (1014–1028 and 1055–1070) repeats the same unavailable selected-track ID, title, roles, and description with a different reason. `unavailable_scientific_context_figures()` publicly returns three figures, but `build_scientific_context_figures()` takes only its first two when context is absent, then constructs the selected track according to candidate availability. A small private builder parameterized only by reason could eliminate the repeated specification, with roughly 10 net product lines in an in-memory sketch. The exported three-item roster and distinct context versus selected-candidate unavailability reasons must survive. No exact replacement diff, caller-complete test, or SVG parity result exists yet. The two large figure owners use different plot semantics, so a broad renderer merger has no measured saving.

### F48 — SVG attribute URL case

`figures._validated_svg` (164–205) parses bounded SVG with ElementTree, rejects active tags, external `href`, and external CSS `url(...)`. Its style-text branch uses `re.IGNORECASE` (189), but the attribute branch at 202 does not. In an isolated standard-library probe extracting the actual function, `<rect fill="URL(https://example.org/a)"/>` and mixed-case `uRl(...)` were admitted while lowercase `url(...)` was rejected; current direct tests cover lowercase external URLs and styles. `_render_svg` hashes the admitted bytes, the figure model checks data-URI base64/size/hash, and the template embeds the URI in an image. The later HTML validators do not decode that SVG payload, so source tracing indicates this lexical gap survives those checks if such bytes enter `_validated_svg`. The probe did not run Matplotlib, render a report, or show a browser request; normal producer output may not contain the case variant. A separate owner repair should add case variants and preserve the output contract. There is no material net-code reduction.

### F49 — Compute receipt root shape

`storage_inventory/qualification.py` `_validate_compute` (480–515) checks that `roots` is a list of the expected length and each row is a dictionary with the expected role. It then assumes `row.get("root", {})` is a dictionary (505); `null` makes `.get("path")` raise `AttributeError`. In a tiny temporary retained-receipt fixture, the public `qualify_from_args(phase="finalize", execute=True)` route printed its plan and then raised that raw exception, rather than returning the route's controlled `StorageQualificationError` exit 2 (881–913). Current compute-receipt mutation tests cover roles, probes, hashes, and roster length but not root-object type. The probe used no Slurm or production artifact and does not imply incorrect qualification. A repair needs malformed retained-object cases through direct and two-phase admission while preserving existing receipt bytes and evidence. It offers no net-code saving.

### F50 — Scientific-context receipt interleaving

`scientific_context.py` `_bound_path` (981–995) checks each receipt path and hashes its bytes. `validate_scientific_context_transaction` (1346–1407) later reopens those same paths for catalog, Step 09 projection, output, reference, and candidate reconciliation, but does not compare final content hashes with the receipt. A deterministic tiny-fixture hook changed one Step 09 summary string from `0.05` to semantically equal `0.050` after its first hash and before projection; the actual transaction function returned an admitted object although the receipt hash no longer matched the current file. This is a forced interleaving, not observed production concurrency or a scientific-result failure. The Step 10 CLI snapshots bound files before admission and checks their identities before publishing a validation report; artifact-index reconciliation compares source hashes and its publisher rechecks source metadata. Those later defenses limit this observation to the core function and leave no demonstrated full Run or published-report bypass. Any repair must preserve the independent semantic checks and external snapshot defenses; it is not a deletion candidate.

### F51 — Step 08 raw VCF count width

`_step_08_vcf_counts.R` `validate_raw_count_value` (34–51) uses `strsplit(value, ",", fixed=TRUE)` to check lexical widths. R drops a trailing empty token, so an isolated call to the actual function accepted `"1,"` for width one and `"1,2,"` for width two. A tiny one-record VCF containing `INFO/AD=25,5,` and `FORMAT/AD=10,2,` also passed the actual `validate_raw_vcf_counts` pass; only its text-connection helper was stubbed for that probe. The [Step 08 contract](../../src/emrys/stages/cohort_candidate_preprocessing/CONTRACT.md) lines 60–65 requires exact raw count widths, and current real-R negatives cover malformed letters and partial missingness but not trailing delimiters. The later VariantAnnotation parser and full Step 08 producer were not exercised; they may reject this input. A separate owner slice should characterize the complete path and reject malformed lexical values at the promised preparse boundary if the gap persists. No scientific outcome or code reduction is claimed.

### F52 — Repeated test guidance

The owner READMEs in `tests/evidence/canonical_bam_qc`, `tests/evidence/rseqc_orientation`, and `tests/stages/{canonical_bam,duplicate_marking,fasta_sidecars,split_n_cigar,star_alignment,star_index}` repeat the same five physical lines about runner-style scratch/staging, the common Task suite's publication and recovery coverage, grouped Python validation, and shared evidence limits. An exact paragraph scan of tracked Markdown found those eight copies (40 lines), while `tests/README.md` already owns the evidence-level explanation. One common paragraph in that test-tree guide plus concise links in the eight owner READMEs could save approximately 20–27 net documentation lines, depending on wrapping and retained local context. Each owner's first paragraph states its distinct protected behavior and must stay. The identical Java thread paragraph in the Step 04 and Step 05 contracts describes each worker's contract and is not included in this candidate. A `DOCS-01` slice should verify navigation and local meaning before any deletion. This is neither product-code nor test-code saving.

### F53 — Step 02 exact read-group fields

The Step 02 worker's `validate_bam_pair` (69–74) and canonical-input reuse predicate (98–101) test whether the raw `@RG` line *contains* `LB:$sample_id` and `PL:ILLUMINA`; they do not split tab-delimited fields or require exact field values. The [owner contract](../../src/emrys/stages/canonical_bam/CONTRACT.md) lines 50–53 and 74–80 requires `LB` equal to the sample and `PL:ILLUMINA` for producer output and no-rewrite reuse. In a tiny temporary worker run against the actual shell script, fake samtools returned one coordinate-sorted `@RG` header with exact `ID:sample`/`SM:sample` but `LB:sampleExtra`/`PL:ILLUMINAX`, positive alignment/tag counts, and successful quickcheck/index. The worker exited zero and staged a hard link to the wrong-metadata input; equal input/output inode confirmed the no-rewrite path. The temporary fixture was removed. Native worker tests cover exact canonical metadata, wrong `SM`, extra read groups, and partial tags but not suffix variants. Shared `parse_header` (`libraries/alignments/bam.py`, 56–65) exactly checks `ID`/`SM` but does not check `LB`/`PL`; an isolated actual-function call reported the probe header as matching. The [Step 02 contract](../../src/emrys/stages/canonical_bam/CONTRACT.md) lines 143–144 explicitly records that narrower post-publication validator policy, so this is a producer admission defect, not evidence of equivalent duplicate checks. The worker file changed only +13/−8 in the provisional comparison, and these faulty checks predate that baseline. No real samtools/BAM, full validation command, or Run was exercised. A separate repair must preserve the intentional hard-link path for genuinely canonical input and the prepublication check.

### F54 — Void tags in report table validation

`_run_report/validation.py` `ReportHTMLInspector.handle_starttag` (258–268) uses `emrys_table_depth` to track nested tags and require a caption for an `emrys-table`. It increments that depth for every start tag, including HTML void elements. `handle_endtag` (326–335) records missing-caption and header-scope errors only when a `</table>` reduces depth to zero. In an isolated probe executing the actual extracted inspector class, a captionless `<table class="emrys-table"><tr><td>text</td></tr></table>` recorded the expected missing-caption error, while adding `<br>` inside the cell left depth at one and recorded none. `validate_rendered_html` inspects `table_errors` (422–425), so that list would miss this fault in the probed parse. The built-in table template always emits a caption; current report tests do not directly exercise a captionless table with a void child. The probe did not construct a complete staged report, and no published incorrect report is established. Repair needs full-fixture characterization without weakening other table, active-resource, or heading checks; no net-code saving is measured.

### F55 — Analysis artifact descriptor shape

`analyses._safe_artifact` (244–275) verifies that `expected_header` is truthy and its members are truthy, but does not require a tuple of nonempty strings. An isolated extraction of the actual function admitted `expected_header="x"` and `(1,)`. It also admitted `exact_data_rows=0` with `allow_header_only=False`; `_validate_descriptor` (356) uses this helper as its output-shape gate. Artifact-index TSV admission compares parsed tuple headers and later refuses zero data rows when header-only output is forbidden (`_text_tabular.py`, 43–49 and 83–89), so the malformed declarations are unfulfillable or fail after installation rather than at module admission. The built-in paired-CMH provider's declarations are valid. This was a function-level probe and source trace, not a full external-provider Project or Run; local `jsonschema` was unavailable. A separate provider-contract decision should reject malformed or contradictory declarations at the earliest supported boundary and preserve existing valid providers. No code reduction is measured.

### F56 — Step 06 assigned-fraction rounding

`mechanical_orientation/producer.py` `_write_counts` (140–169) calculates an exact integer ratio and formats `assigned_fraction` with six decimal places. The validator (`mechanical_orientation/validator.py`, 103–113) subtracts the parsed float from the binary floating-point ratio and requires an error at most `0.0000005`. For assigned/input counts of `3/128`, the producer writes `0.023438`; the binary-float difference is slightly above the threshold. In a tiny function-level probe, the actual `_write_counts` with a stubbed samtools-count sequence wrote that row, and the actual `build_validation_report` accepted structure and orientation-specific count checks but marked `assigned_count_arithmetic=fail`. This is a generated-row false failure under the fixture, not a real BAM or full Run result. Current owner tests cover a wrong fraction but not the exact halfway boundary. The producer file changed +9/−4 in the provisional comparison, but this formatting expression predates that baseline. A separate repair needs deterministic decimal or scaled-integer comparison and tests on both sides of rounding, without weakening integer count reconciliation. It offers no measured REDUCE saving.

## Coverage and remaining comparisons

This source pass has examined the major changed orchestration owners and representatives from every product family: reporting, contracts, analyses, stages, evidence, libraries, workflow, and ingestion, plus scripts, configuration, schemas, tests, and documentation. It has **not** completed a file-by-file caller and fault audit of all 203 product files. The 149 files unchanged by the provisional integration diff remain within REDUCE-01 scope. Among them, reporting has 39 files/9,156 lines, contracts 20/4,903, stages 28/3,949, and analyses 12/3,781: together 21,789 lines, or 80.2% of unchanged product stock. A deeper pass examined three unchanged scientific contract files (3,389 lines), two figure owners (2,333 lines), the 929-line storage qualifier, and Step 08's six R files (1,829 lines). It found no large, caller-complete retirement: Step 08 and Step 09 sample-value checks have similar DP/AD/AF rules but different diagnostic order and independent projection/receipt duties; paired figure renderers have different plot semantics; storage direct and two-phase receipt checks bind different host/placement facts. The 571-line analysis-module API has active installed entry-point, profile, identity, and readmission callers, so its size alone is not a facade-retirement case. An exact AST-body screen across 174 tracked Python files found three repeated function bodies of at least six lines: a stage-producer nonempty-file predicate, an HTML parser callback, and the nested TSV row loops already covered by F23. Sharing the first two would need error or owner adapters and has no measured net saving; the screen does not detect near duplicates or non-Python code. The five unchanged orchestration files contain 868 lines; their substantive owners have active callers and distinct roles. The rest still need owner-by-owner comparison. The next comparison must prioritize high-growth coordinator branches, dynamic/package consumers of retirement candidates, exact fault behavior, and measurable replacement diffs. `SIZE-01`, `SCHEMA-01`, `ASSURANCE-01`, `DOCS-01`, and `OPS-03` retain their own decisions and acceptance.

A separate broad source screen covered 47 Python modules of at least 350 lines and exact 12-line windows in product Python/R/Shell. The largest repeated individual block was 18 lines; the largest repeated pair totaled 50 lines and is the Step 09/10 R candidate F02. Apparent static import orphans had live dynamic or public consumers: the planner invokes stage producers by `python -m`, the analysis and reporter packages are installed entry points, `__main__.py` dispatches the console command, and the synthetic fixture is a supported Init route. The seven largest coordinator Python modules contain 17,386 current lines (26.7% of product stock), but current size is not a saving. This screen gives no new caller-complete candidate of at least 100 net product lines; exact repetition and import graphs can miss semantic overlap or external consumers.

## Selection boundary

Before a reduction slice, approve exact comparison refs, counted product extensions, and move treatment for the selected net-growth metric; select one bounded, caller-complete outcome; identify all preserved behavior and protections; and measure product code, tests, tooling, schemas, configuration, documentation, mutable state, and retained evidence separately. The current source pass does not establish a thousands-of-lines saving. Site and scientific evidence remain at their existing levels.
