# E2E-03A — Prove fresh-clone local pilot

## Outcome

A genuinely fresh clone completes the fixed local pilot with deterministic
no-science owners, including a clean success and a separate controlled
between-task failure followed by byte-preserving resume and final inspection.

## Touches

The closed write scope is the top-level CLI dependency seam, local-pilot E2E
tests and test fixtures, public local-pilot configuration starters and their
direct tests/catalog entry, one durable testing-history record and index, and
the directly affected B6 status documentation. The user-owned untracked
`docs/quickstart.md` and `fix/no-clobber` are excluded.

## Stop

Stop if proof would require a shipped fake mode, raw Snakemake escape hatch,
private production import, real science-tool or R restoration, cluster/VM/SLURM
work, production data, destructive cleanup, evidence promotion, or a change to
scientific, transaction, locking, no-clobber, recovery, or reporting semantics.
Stop and re-plan on any correctness defect outside the approved B6 boundary.

## Context

B5 exposes dry-run-first public `doctor`, `run`, `resume`, and `inspect`
commands over the fixed source-checkout profile. Existing no-science execution
tests exercise explicit `ControlOps`, but do not start from a genuine clone or
prove the top-level parser route. The generic sample starter cannot satisfy the
local pilot's paired-replicate intake contract.

## Deliverables

- One explicit top-level parser dependency seam whose production default is
  unchanged.
- One fresh-clone E2E with exact locked setup, deterministic fixture identity,
  public readiness/dry-run/inspection routes, clean completion, controlled
  between-task failure, no-write resume planning, byte-preserving resume,
  reporting outputs, and refusal behavior.
- Structural request, sample, and partition starters that satisfy local-pilot
  intake while pointing only to operator-replaced paths.
- One revision- and fixture-bound durable evidence record.

## Acceptance evidence

Run focused CLI, intake, materialization, and fresh-clone tests; verify the
clone remains clean and independent; verify 34 owner jobs and three reporting
transactions; re-admit final state and HTML/report receipts; verify Step `09c`
remains absent and evidence-incomplete; then run the assembled local gate once
after the final B6 state. Label all results as no-science local control-plane
evidence only.

## Documentation updates

Update the configuration catalog, local-pilot/workflow boundary documentation,
testing-history index, handoff, and pipeline state with the proven result.
Remove this card on E2E completion, then select `ONBOARD-03A` separately and
rewrite root onboarding from the exact proven transcript.
