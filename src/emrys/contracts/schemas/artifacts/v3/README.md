# Run result manifest schema

[`run_summary.schema.json`](run_summary.schema.json) defines the current v4 Run result manifest. It binds the immutable Run contract and module policy, explicit inventory, ordered artifact entries, computational projections and publication identity. No historical summary or report receipt reader is registered.
