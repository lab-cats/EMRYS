# Packaged resources

This directory contains non-Python resources distributed with EMRYS. The `runtime/` subtree holds the locked managed-environment definition and runtime admission policy consumed by discovery and Doctor; it is package data, not a user-editable Project runtime or site profile.
