# Packaged resources

`runtime/` holds the locked managed-environment definition and runtime checking
policy used by discovery and Doctor. These files ship with EMRYS; they are not
user-editable Project runtime or site profiles.
